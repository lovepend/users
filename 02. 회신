<template>
  <div class="main-layout">
    <header class="main-header">
      <div class="header-content">
        <h1 class="header-title">컴포넌트 라이브러리 가이드</h1>
        <div class="header-badges">
          <span class="badge primary">Vue 3</span>
          <span class="badge outline">Standard UI</span>
        </div>
      </div>
      
      <nav class="quick-nav">
        <a href="#section-title" class="nav-item">PageTitle</a>
        <a href="#section-card" class="nav-item">Cards</a>
        <a href="#section-form" class="nav-item">Forms</a>
        <a href="#section-action" class="nav-item">Actions</a>
      </nav>
    </header>

    <main class="content-body">
      <div class="content-inner">
        
        <section id="section-title" class="content-section">
          <div class="section-header">
            <h2 class="section-label">PageTitle & Global Actions</h2>
          </div>
          <div class="demo-card">
            <PageTitle icon="fa-lock" title="시스템 설정" style="width: 100%">
              <template #sub-content>
                <CheckBox
                  id="cb-edit-mode"
                  label="편집 활성화"
                  v-model:value="checkDemo.agree"
                />
              </template>
              <template #actions>
                <div class="action-group">
                  <InputBox
                    id="in-search"
                    placeholder="검색어를 입력하세요..."
                    v-model:value="formDemo.inputText"
                    icon="fa-search"
                  />
                  <SelectBox
                    :list="formDemo.selectList"
                    v-model:value="formDemo.selectId"
                    placeholder="필터 선택"
                  />
                  <ButtonsComponent :buttons="buttonData0" @button-click="handleButtonClick" />
                </div>
              </template>
            </PageTitle>
          </div>
        </section>

        <section id="section-card" class="content-section">
          <div class="section-header">
            <h2 class="section-label">Card View Examples</h2>
          </div>
          <div class="card-grid">
            <DefaultCard
              :title="example1.title"
              :tabs="example1.tabs"
              v-model:active-tab="activeTab1"
              @tab-change="onTabChange('재고', $event)"
            >
              <div class="card-content-box">
                <div class="mini-table">
                  <div class="mini-table-head">
                    <span>코드</span><span>품목명</span><span>수량</span><span>상태</span>
                  </div>
                  <div v-for="row in miniRows" :key="row.code" class="mini-table-row">
                    <span class="txt-code">{{ row.code }}</span>
                    <span class="txt-name">{{ row.name }}</span>
                    <span class="txt-num">{{ row.qty }}</span>
                    <span :class="['status-pill', row.status === '정상' ? 'success' : 'warn']">
                      {{ row.status }}
                    </span>
                  </div>
                </div>
              </div>
            </DefaultCard>

            <DefaultCard title="데이터 요약" class="summary-card">
              <template #header-right>
                <SwitchBox :list="formDemo.switchListDisabled" v-model:value="formDemo.switchId2" />
              </template>
              <div class="placeholder-content">
                <i class="fa fa-chart-line"></i>
                <p>데이터 분석을 위해 탭을 선택해주세요.</p>
              </div>
            </DefaultCard>
          </div>
        </section>

        <section id="section-form" class="content-section">
          <div class="section-header">
            <h2 class="section-label">Form Controls</h2>
          </div>
          <div class="form-row-grid">
            <div class="input-field">
              <label>텍스트 입력</label>
              <InputBox v-model:value="formDemo.inputMemo" placeholder="메모를 남겨주세요." />
            </div>
            <div class="input-field">
              <label>상태 선택</label>
              <SelectBox :list="formDemo.selectList" v-model:value="formDemo.selectId" />
            </div>
            <div class="input-field">
              <label>알림 설정</label>
              <SwitchBox :list="formDemo.switchList" v-model:value="formDemo.switchId" />
            </div>
          </div>
        </section>

      </div>
    </main>
  </div>
</template>

<script setup>
/* 기존에 쓰시던 script 로직 그대로 붙여넣으시면 됩니다 */
import { reactive, ref } from 'vue'
import PageTitle from '@/components/toolBar/PageTitle.vue'
import ButtonsComponent from '@/components/editors/button/ButtonsComponent.vue'
import DefaultCard from '@/components/default/DefaultCard.vue'
import CheckBox from '@/components/default/editors/CheckBox.vue'
import SelectBox from '@/components/default/editors/SelectBox.vue'
import InputBox from '@/components/default/editors/InputBox.vue'
import SwitchBox from '@/components/default/editors/SwitchBox.vue'

// 데이터 및 이벤트 로직 (생략 - 기존 코드 유지)
const activeTab1 = ref('list')
const checkDemo = reactive({ agree: true })
const formDemo = reactive({
  inputText: '',
  selectId: 'opt-1',
  selectList: [{ id: 'opt-1', label: '옵션 01' }, { id: 'opt-2', label: '옵션 02' }],
  switchList: [{ id: 'on', label: 'ON' }, { id: 'off', label: 'OFF' }],
  switchListDisabled: [{ id: 'a', label: 'A' }, { id: 'b', label: 'B' }]
})
const buttonData0 = ref([{ id: 'save', text: '저장', color: 'var(--btn03)', icon: 'fa-save' }])
const miniRows = [
  { code: 'A01', name: '아이템 1', qty: 10, status: '정상' },
  { code: 'A02', name: '아이템 2', qty: 0, status: '품절' }
]

const handleButtonClick = (e, id) => console.log(id)
const onTabChange = (n, id) => console.log(n, id)
</script>

<style scoped>
/* 핵심 레이아웃 스타일 */
.main-layout {
  display: flex;
  flex-direction: column;
  width: 100%;
  height: 100vh;
  background-color: #f4f7fa; /* 연한 배경색으로 컴포넌트 강조 */
  color: #333;
}

.main-header {
  background: #fff;
  border-bottom: 1px solid #e1e4e8;
  padding: 20px 40px 0;
  z-index: 10;
}

.header-content {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 16px;
}

.header-title {
  font-size: 20px;
  font-weight: 700;
  margin: 0;
}

.quick-nav {
  display: flex;
  gap: 24px;
}

.nav-item {
  padding: 12px 4px;
  font-size: 14px;
  color: #666;
  text-decoration: none;
  border-bottom: 2px solid transparent;
  transition: all 0.2s;
}

.nav-item:hover {
  color: var(--primary-color, #4a90e2);
  border-bottom-color: var(--primary-color, #4a90e2);
}

.content-body {
  flex: 1;
  overflow-y: auto;
  padding: 30px 40px;
}

.content-inner {
  max-width: 1400px;
  margin: 0 auto;
}

.content-section {
  margin-bottom: 40px;
}

.section-label {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 16px;
  color: #555;
}

.demo-card {
  background: #fff;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.05);
}

/* 액션 그룹 (버튼, 인풋 등 나란히 배치) */
.action-group {
  display: flex;
  align-items: center;
  gap: 10px;
}

/* 카드 그리드 */
.card-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr;
  gap: 20px;
}

/* 미니 테이블 스타일 */
.mini-table {
  border: 1px solid #eee;
  border-radius: 4px;
}

.mini-table-head {
  display: grid;
  grid-template-columns: 80px 1fr 60px 80px;
  background: #f8f9fa;
  padding: 10px;
  font-weight: 600;
  font-size: 13px;
  border-bottom: 1px solid #eee;
}

.mini-table-row {
  display: grid;
  grid-template-columns: 80px 1fr 60px 80px;
  padding: 10px;
  border-bottom: 1px solid #f9f9f9;
  font-size: 13px;
  align-items: center;
}

.status-pill {
  font-size: 11px;
  padding: 2px 6px;
  border-radius: 10px;
  text-align: center;
}
.status-pill.success { background: #e6f7ed; color: #1f8b4c; }
.status-pill.warn { background: #fff4e5; color: #b76e00; }

/* 폼 그리드 */
.form-row-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  background: #fff;
  padding: 24px;
  border-radius: 8px;
}

.input-field {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.input-field label {
  font-size: 13px;
  font-weight: 600;
  color: #666;
}

.badge {
  font-size: 12px;
  padding: 2px 8px;
  border-radius: 4px;
}
.badge.primary { background: #4a90e2; color: #fff; }
.badge.outline { border: 1px solid #4a90e2; color: #4a90e2; }

@media (max-width: 1024px) {
  .card-grid { grid-template-columns: 1fr; }
}
</style>
