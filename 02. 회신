<template>
  <div class="component-template">
    <header class="template__page-head">
      <div class="template__title-wrapper">
        <h1 class="template__page-title">컴포넌트 가이드</h1>
        <span class="tpl-badge">v1.2</span>
      </div>
      <p class="template__page-desc">공통 컴포넌트의 사용 예시와 UI 레이아웃 가이드를 제공합니다.</p>
    </header>

    <div class="template__body">
      <nav class="template__toc" aria-label="페이지 섹션">
        <a class="template__toc-link" href="#tpl-section-pageTitle">PageTitle</a>
        <a class="template__toc-link" href="#tpl-section-cards">Cards</a>
        <a class="template__toc-link" href="#tpl-section-buttons">Buttons</a>
        <a class="template__toc-link" href="#tpl-section-inputs">Inputs</a>
        <a class="template__toc-link" href="#tpl-section-toast">Toast</a>
      </nav>

      <div class="template__stack">
        <section class="template__section template__section--first" id="tpl-section-pageTitle">
          <h2 class="template__section-title">PageTitle & Global Actions</h2>
          <div class="tpl-demo-container">
            <PageTitle icon="fa-laptop-code" title="프로젝트 관리" style="width: 100%">
              <template #sub-content>
                <CheckBox
                  id="tpl-cb-agree"
                  label="필터 잠금"
                  v-model:value="checkDemo.agree"
                  @change="onCheckDemoChange('agree', $event)"
                />
              </template>
              <template #actions>
                <div class="tpl-action-group">
                  <InputBox
                    id="tpl-in-keywords"
                    v-model:value="formDemo.inputText"
                    placeholder="검색어 입력..."
                    icon="fa-search"
                    @input="onFormInputDemo"
                    @clear="onFormInputClear"
                  />
                  <SelectBox
                    :list="formDemo.selectList"
                    v-model:value="formDemo.selectId"
                    placeholder="카테고리"
                    @change="onFormSelectChange"
                  />
                  <ButtonsComponent :buttons="buttonData1" @button-click="handleButtonClick" />
                </div>
              </template>
            </PageTitle>
          </div>
        </section>

        <section id="tpl-section-cards" class="template__section">
          <h2 class="template__section-title">DefaultCard Layout</h2>
          <p class="template__section-desc">표준 카드 레이아웃으로 헤더, 탭, 액션바를 포함합니다.</p>

          <div class="template__grid">
            <div class="template__demo">
              <DefaultCard
                :title="example1.title"
                :tabs="example1.tabs"
                v-model:active-tab="activeTab1"
                class="template__card"
                @tab-change="onTabChange('예시1', $event)"
              >
                <template #header-right>
                  <ButtonsComponent :buttons="buttonData0" @button-click="handleButtonClick" />
                </template>
                <template #actions>
                  <div class="tpl-card-actions">
                    <CheckBox id="tpl-cb-news" label="상태 요약 보기" v-model:value="checkDemo.news" />
                    <SwitchBox :list="formDemo.switchList" v-model:value="formDemo.switchId" />
                  </div>
                </template>
                
                <div class="tpl-mini-grid">
                  <div class="tpl-mini-grid__head tpl-mini-grid__row">
                    <span>코드</span><span>항목</span><span>수량</span><span>상태</span>
                  </div>
                  <div v-for="row in miniRows" :key="row.code" class="tpl-mini-grid__row">
                    <span>{{ row.code }}</span>
                    <span>{{ row.name }}</span>
                    <span class="tpl-num">{{ row.qty }}</span>
                    <span :class="['tpl-status', row.status === '정상' ? 'is-ok' : 'is-warn']">{{ row.status }}</span>
                  </div>
                </div>
              </DefaultCard>
            </div>
          </div>
        </section>

        <section class="template__section" id="tpl-section-inputs">
          <h2 class="template__section-title">Form Controls</h2>
          <div class="tpl-form-grid">
            <div class="tpl-form-item">
              <label class="tpl-label">SelectBox</label>
              <SelectBox :list="formDemo.selectList" v-model:value="formDemo.selectId" @change="onFormSelectChange" />
            </div>
            <div class="tpl-form-item">
              <label class="tpl-label">InputBox</label>
              <InputBox v-model:value="formDemo.inputMemo" placeholder="메모를 입력하세요" />
            </div>
            <div class="tpl-form-item">
              <label class="tpl-label">SwitchBox</label>
              <SwitchBox :list="formDemo.switchListDisabled" v-model:value="formDemo.switchId2" />
            </div>
          </div>
        </section>

        <section class="template__section" id="tpl-section-toast">
          <h2 class="template__section-title">Global Toast</h2>
          <div class="tpl-toast-demo">
            <div class="tpl-toast-demo__buttons">
              <button type="button" class="tpl-btn tpl-btn--primary" @click="showTplToastSuccess">Success</button>
              <button type="button" class="tpl-btn tpl-btn--danger" @click="showTplToastFail">Error</button>
              <button type="button" class="tpl-btn tpl-btn--warning" @click="showTplToastWarning">Confirm Modal</button>
              <button type="button" class="tpl-btn tpl-btn--ghost" @click="showTplToastConfirm">Delete Dialog</button>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
</template>

<style scoped>
  .component-template {
    --tpl-divider: var(--border01, rgba(0, 0, 0, 0.08));
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    max-width: 1280px;
    margin: 0 auto;
    overflow: hidden;
    background: var(--bg01);
  }

  /* 헤더 디자인 */
  .template__page-head {
    padding: 24px 32px;
    background: var(--bg01);
  }

  .template__title-wrapper {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 8px;
  }

  .template__page-title {
    margin: 0;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--txt03);
  }

  .tpl-badge {
    padding: 2px 8px;
    font-size: 0.75rem;
    background: var(--primary-color);
    color: #fff;
    border-radius: 4px;
  }

  .template__page-desc {
    color: var(--txt13);
    font-size: 0.9rem;
  }

  /* 네비게이션(TOC) */
  .template__toc {
    display: flex;
    gap: 12px;
    padding: 0 32px 16px;
    border-bottom: 1px solid var(--tpl-divider);
  }

  .template__toc-link {
    padding: 6px 16px;
    font-size: 0.85rem;
    text-decoration: none;
    color: var(--txt13);
    border-radius: 20px;
    border: 1px solid var(--tpl-divider);
    transition: all 0.2s;
  }

  .template__toc-link:hover {
    border-color: var(--primary-color);
    color: var(--primary-color);
  }

  /* 메인 스크롤 영역 */
  .template__stack {
    flex: 1;
    overflow-y: auto;
    padding: 32px;
    scroll-behavior: smooth;
  }

  .template__section {
    margin-bottom: 48px;
  }

  .template__section-title {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 20px;
    padding-left: 10px;
    border-left: 4px solid var(--primary-color);
    color: var(--txt03);
  }

  /* 데모 컨테이너 */
  .tpl-demo-container {
    padding: 20px;
    background: var(--card-bg);
    border: 1px solid var(--tpl-divider);
  }

  .tpl-action-group {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .tpl-card-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
  }

  /* 미니 그리드 스타일 */
  .tpl-mini-grid {
    display: grid;
    grid-template-columns: 100px 1fr 80px 80px;
    border: 1px solid var(--tpl-divider);
  }

  .tpl-mini-grid__row > span {
    padding: 10px 15px;
    border-bottom: 1px solid var(--tpl-divider);
    font-size: 0.85rem;
  }

  .tpl-mini-grid__head {
    background: var(--bg02, #f9f9f9);
    font-weight: 600;
  }

  .tpl-status.is-warn { color: var(--btn04); font-weight: 600; }
  .tpl-status.is-ok { color: var(--primary-color); }

  /* 폼 그리드 */
  .tpl-form-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 20px;
  }

  .tpl-form-item {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .tpl-label {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--txt13);
  }

  /* 버튼 스타일 */
  .tpl-btn {
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    font-weight: 500;
    border: none;
  }

  .tpl-btn--primary { background: var(--primary-color); color: white; }
  .tpl-btn--danger { background: var(--btn04); color: white; }
  .tpl-btn--warning { background: var(--btn07); color: white; }
  .tpl-btn--ghost { background: transparent; border: 1px solid var(--tpl-divider); }

  /* 반응형 */
  @media (max-width: 768px) {
    .tpl-action-group { flex-direction: column; align-items: stretch; }
    .template__toc { overflow-x: auto; white-space: nowrap; }
    .template__page-head { padding: 16px; }
    .template__stack { padding: 16px; }
  }
</style>
