<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism-tomorrow.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.css" rel="stylesheet" />
    <style>
        :root { --bg-color: #1e1e1e; --header-bg: #2d2d2d; --accent-color: #007acc; }
        body, html { margin: 0; padding: 0; height: 100%; background-color: var(--bg-color); color: #ccc; font-family: 'Consolas', monospace; overflow: hidden; }
        
        .top-bar { height: 35px; background: var(--header-bg); display: flex; justify-content: space-between; align-items: center; padding: 0 15px; border-bottom: 1px solid #3e3e3e; box-sizing: border-box; }
        .logo { font-size: 11px; color: #858585; letter-spacing: 1px; }
        .btn-group { display: flex; gap: 6px; }
        button { background: #3c3c3c; color: #fff; border: 1px solid #454545; padding: 2px 10px; border-radius: 2px; cursor: pointer; font-size: 11px; height: 22px; }
        button#saveBtn { background: var(--accent-color); border: none; }
        button:disabled { opacity: 0.3; }

        .main-container { position: relative; height: calc(100% - 35px); width: 100%; }

        textarea, #code-display {
            position: absolute; width: 100%; height: 100%;
            top: 0; left: 0; border: none; outline: none;
        }

        textarea {
            padding: 10px 20px 10px 55px; /* 에디터에서도 숫자 영역만큼 띄움 */
            box-sizing: border-box;
            background: var(--bg-color); color: #d4d4d4;
            font-size: 14px; line-height: 1.6; font-family: inherit;
            resize: none; white-space: pre; overflow: auto;
            z-index: 10; 
        }

        #code-display {
            z-index: 1; background: var(--bg-color);
            overflow: auto; visibility: hidden;
        }
        #code-display.active { visibility: visible; z-index: 20; }

        /* --- Prism 레이아웃 핵심 수정 --- */
        pre[class*="language-"].line-numbers {
            margin: 0 !important;
            padding: 10px 20px 10px 55px !important; /* 숫자 바를 위한 공간 55px 확보 */
            background: var(--bg-color) !important;
            height: 100% !important;
            box-sizing: border-box !important;
            white-space: pre-wrap !important;
            word-break: break-all !important;
        }

        code[class*="language-"] {
            white-space: pre-wrap !important;
            word-break: break-all !important;
            font-family: inherit !important;
            position: relative;
        }

        /* 숫자 라인 위치 강제 조정 */
        .line-numbers .line-numbers-rows {
            position: absolute;
            pointer-events: none;
            top: 10px !important;      /* 패딩 탑 10px 고정 */
            left: -45px !important;    /* 왼쪽으로 확 밀어냄 (음수값 사용) */
            width: 40px !important;    /* 숫자 영역 너비 고정 */
            letter-spacing: -1px;
            border-right: 1px solid #444 !important; /* 구분선 */
            user-select: none;
        }

        /* 개별 숫자 텍스트 정렬 */
        .line-numbers-rows > span:before {
            padding-right: 8px !important;
            text-align: right !important;
            color: #757575 !important;
        }
    </style>
</head>
<body>

<div class="top-bar">
    <div class="logo">NOTE</div>
    <div class="btn-group">
        <button id="editBtn">Edit (Ctrl+E)</button>
        <button id="saveBtn" disabled>Save (Ctrl+S)</button>
    </div>
</div>

<div class="main-container">
    <textarea id="editor" spellcheck="false" placeholder="Loading..."></textarea>
    <div id="code-display">
        <pre id="pre-area" class="line-numbers"><code id="highlight-area" class="language-javascript"></code></pre>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-javascript.min.js"></script>

<script>
    const editor = document.getElementById('editor');
    const codeDisplay = document.getElementById('code-display');
    const highlightArea = document.getElementById('highlight-area');
    const editBtn = document.getElementById('editBtn');
    const saveBtn = document.getElementById('saveBtn');
    
    const STORAGE_KEY = 'vsc_note_v8_final_layout';

    window.onload = async () => {
        const saved = localStorage.getItem(STORAGE_KEY);
        if (saved) {
            editor.value = saved;
        } else {
            editor.value = "// 코드를 복사 붙여넣기 해보세요.\nfunction test() {\n    console.log('Line numbers fixed');\n}";
        }
        setMode(false);
    };

    function updateHighlight() {
        highlightArea.textContent = editor.value;
        Prism.highlightElement(highlightArea);
        
        // 렌더링 후 레이아웃 깨짐 방지
        setTimeout(() => {
            window.dispatchEvent(new Event('resize'));
        }, 50);
    }

    function setMode(isEdit) {
        if (isEdit) {
            codeDisplay.classList.remove('active');
            editor.style.visibility = "visible";
            saveBtn.disabled = false;
            editBtn.textContent = "View";
            editor.focus();
        } else {
            updateHighlight();
            editor.style.visibility = "hidden";
            codeDisplay.classList.add('active');
            saveBtn.disabled = true;
            editBtn.textContent = "Edit";
        }
    }

    function save() {
        localStorage.setItem(STORAGE_KEY, editor.value);
        saveBtn.textContent = "Saved!";
        setTimeout(() => {
            saveBtn.textContent = "Save";
            setMode(false);
        }, 600);
    }

    editBtn.addEventListener('click', () => {
        const isEditing = editor.style.visibility === "visible";
        setMode(!isEditing);
    });
    
    saveBtn.addEventListener('click', save);

    window.addEventListener('keydown', (e) => {
        const isCtrl = e.ctrlKey || e.metaKey;
        if (isCtrl && e.key.toLowerCase() === 'e') { e.preventDefault(); editBtn.click(); }
        if (isCtrl && e.key.toLowerCase() === 's') { e.preventDefault(); save(); }
    });

    editor.addEventListener('keydown', function(e) {
        if (e.key === 'Tab') {
            e.preventDefault();
            const start = this.selectionStart;
            this.value = this.value.substring(0, start) + "    " + this.value.substring(this.selectionEnd);
            this.selectionStart = this.selectionEnd = start + 4;
        }
    });
</script>
</body>
</html>
