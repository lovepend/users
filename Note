{
  field: 'VALUE',
  headerName: 'VALUE',
  minWidth: 160,
  flex: 2,
  editable: true,
  cellDataType: 'object',
  valueParser: params => {
    const { newValue, data } = params;
    if (!newValue) return null;
    
    // OPER이 EQ일 때는 공백을 자르지 않고 분리만 수행
    if (data.OPER === 'EQ') {
      return newValue.split(',');
    }
    return newValue.split(',').map(item => item.trim());
  },
  valueFormatter: params => {
    const isPbsField = params.data.ATT_ID === 'ASSEMBLY_NAME' || params.data.ATT_ID === 'SYSTEM_NAME';
    const isEq = params.data.OPER === 'EQ'; // EQ 여부 확인

    if (isPbsField) {
      if (!params.value || (typeof params.value === 'string' && params.value.trim() === '')) {
        return '';
      }

      let tagIdxsToConvert = [];
      if (typeof params.value === 'string') {
        // EQ인 경우 trim() 제외
        tagIdxsToConvert = params.value.split(',').map(item => isEq ? item : item.trim());
      } else if (Array.isArray(params.value)) {
        tagIdxsToConvert = params.value.map(item => isEq ? String(item) : String(item).trim());
      } else {
        const val = String(params.value);
        tagIdxsToConvert = [isEq ? val : val.trim()];
      }

      const transformedCodes = tagIdxsToConvert
        .map(tagIdx => getCodeByTagIdx(tagIdx))
        .filter(code => code !== undefined && code !== null);

      return transformedCodes.join(isEq ? ',' : ', '); // EQ일 때는 콤마 뒤 공백 없이 결합 가능
    }

    return Array.isArray(params.value) ? params.value.join(isEq ? ',' : ', ') : params.value;
  },
  
  // ... 중간 cellEditorSelector 생략 ...

  valueSetter: params => {
    const val_type = LinkedAttrListRef.value.find(elem => elem.ATT_ID == params.data.ATT_ID)?.VAL_TYPE;
    if (val_type === 'BOOL') {
      params.data.VALUE = params.newValue;
      return true;
    }

    const { newValue, data } = params;
    const isPbsField = data.ATT_ID === 'ASSEMBLY_NAME' || data.ATT_ID === 'SYSTEM_NAME';
    const isEq = data.OPER === 'EQ'; // EQ 조건 변수
    let valueToStore = newValue;

    if (isPbsField) {
      let codesToConvert = [];

      if (typeof newValue === 'string' && newValue !== '') {
        // EQ일 때는 trim()을 하지 않음
        codesToConvert = newValue.split(',').map(item => isEq ? item : item.trim());
      } else if (Array.isArray(newValue)) {
        codesToConvert = newValue.map(item => isEq ? String(item) : String(item).trim());
      } else if (newValue === null || newValue === undefined) {
        codesToConvert = [];
      }

      if (codesToConvert.length > 0) {
        const targetComboData = data.ATT_ID === 'ASSEMBLY_NAME' ? assemblyPbsComboData.value : systemPbsComboData.value;

        const tagIdxArray = codesToConvert.map(code => {
          // 비교 시에도 EQ일 때는 공백이 포함된 상태로 찾음
          const found = Object.values(targetComboData).find(item => item.display === code);
          return found ? found.value : code;
        });

        valueToStore = tagIdxArray.join(',');
      } else {
        valueToStore = '';
      }
    }

    // 최종 저장 단계에서 공백 제거 로직 수정
    if (typeof valueToStore === 'string' && valueToStore !== '') {
      const parts = valueToStore.split(',');
      if (isEq) {
        // EQ일 때는 빈 값만 필터링하고 공백(trim)은 유지
        valueToStore = parts.filter(s => s.length > 0).join(',');
      } else {
        // EQ가 아닐 때는 기존처럼 공백 제거
        valueToStore = parts.map(s => s.trim()).filter(s => s.length > 0).join(',');
      }
    }
    
    data.VALUE = valueToStore;
    return true;
  },
  // ... 하단 생략 ...
}
