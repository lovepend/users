<template>
  <div id="app" class="admin-tool">
    <div class="main-layout">
      <header class="dashboard-header">
        <div class="search-grid">
          <div class="project-box">
            <div class="input-group">
              <label>Project No</label>
              <input v-model="projectNo" type="text" placeholder="P0001" class="base-input" />
            </div>
            <div class="filter-wrapper">
              <label class="sub-label">결과 내 실시간 필터</label>
              <input v-model="localSearchQuery" type="text" placeholder="Tag No 검색..." class="filter-input" />
            </div>
          </div>
          
          <div class="keyword-box">
            <label>API Fetch Keywords</label>
            <textarea v-model="keyword" placeholder="TAG-01, TAG-02..." class="base-input keyword-input"></textarea>
          </div>
          
          <div class="button-box">
            <button @click="fetchAndMergeData" :disabled="isLoading" class="btn-fetch-large">
              <span v-if="!isLoading">데이터 통합 조회하기</span>
              <span v-else class="loader"></span>
            </button>
          </div>
        </div>
      </header>

      <div class="action-bar" v-if="mergedList.length > 0">
        <div class="stats">조회된 그룹: <b>{{ filteredList.length }}</b>건</div>
        <div class="right-buttons">
          <button @click="deleteSelected" class="btn btn-danger" :disabled="selectedIds.length === 0">🗑 선택 삭제</button>
          <button @click="downloadExcel" class="btn btn-success">📥 CSV 내보내기</button>
        </div>
      </div>

      <div class="table-container">
        <table class="modern-table">
          <thead>
            <tr class="main-header">
              <th rowspan="2" class="sticky-col col-chk border-bottom-none">
                <div class="center-content"><input type="checkbox" :checked="isAllSelected" @change="toggleAllSelect" /></div>
              </th>
              <th rowspan="2" class="sticky-col col-idx border-bottom-none">Idx</th>
              <th rowspan="2" class="sticky-col col-tag border-bottom-none">Tag No</th>
              <th colspan="3" class="pub-header-top">Published (Master)</th>
              <th colspan="3" class="wrk-header-top">Working (Current)</th>
            </tr>
            <tr class="sub-header">
              <th class="pub-sub">EP ID</th><th class="pub-sub">CLS</th><th class="pub-sub">Deleted</th>
              <th class="wrk-sub">EP ID</th><th class="wrk-sub">CLS</th><th class="wrk-sub">Deleted</th>
            </tr>
          </thead>
          
          <tbody v-for="group in filteredList" :key="group.TagIdx" class="group-tbody">
            <tr v-for="(rowNum) in Math.max(group.pubList.length, group.wrkList.length, 1)" :key="group.TagIdx + '-' + rowNum">
              
              <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-chk group-fixed border-right">
                <div class="cell-content center"><input type="checkbox" :value="group.TagIdx" v-model="selectedIds" /></div>
              </td>
              <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" 
                  class="sticky-col col-idx group-fixed clickable border-right" @click="openModal(group)">
                <div class="cell-content center font-bold">{{ group.TagIdx }}</div>
              </td>
              <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" 
                  class="sticky-col col-tag group-fixed clickable border-bold-right" @click="openModal(group)">
                <div class="cell-content tag-text" v-html="highlightText(group.TagNo)"></div>
              </td>

              <td class="cell-pub clickable" @click="openModal(group)">
                <div class="cell-content">{{ group.pubList[rowNum-1]?.EpId || '-' }}</div>
              </td>
              <td class="cell-pub clickable" @click="openModal(group)">
                <div class="cell-content">{{ group.pubList[rowNum-1]?.ClsId || '-' }}</div>
              </td>
              <td class="cell-pub clickable" @click="openModal(group)">
                <div class="cell-content center">
                  <span v-if="group.pubList[rowNum-1]" :class="['badge', group.pubList[rowNum-1].Deleted ? 'red' : 'green']">
                    {{ group.pubList[rowNum-1].Deleted }}
                  </span>
                  <span v-else>-</span>
                </div>
              </td>
              
              <td class="cell-wrk clickable" @click="openModal(group)">
                <div class="cell-content">{{ group.wrkList[rowNum-1]?.EpId || '-' }}</div>
              </td>
              <td class="cell-wrk clickable" @click="openModal(group)">
                <div class="cell-content">{{ group.wrkList[rowNum-1]?.ClsId || '-' }}</div>
              </td>
              <td class="cell-wrk clickable" @click="openModal(group)">
                <div class="cell-content center">
                  <span v-if="group.wrkList[rowNum-1]" :class="['badge', group.wrkList[rowNum-1].Deleted ? 'red' : 'green']">
                    {{ group.wrkList[rowNum-1].Deleted }}
                  </span>
                  <span v-else>-</span>
                </div>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <div v-if="showModal" class="modal-overlay" @click.self="closeModal">
      <div class="modal-content">
        <div class="modal-header">
          <h3>상세 정보 확인 <small>#{{ selectedItem?.TagNo }}</small></h3>
          <button @click="closeModal" class="btn-close">&times;</button>
        </div>
        <div class="modal-body">
          <div class="modal-grid">
            <div class="data-pane">
              <h4 class="pub-title">Published Data</h4>
              <pre>{{ JSON.stringify(selectedItem?.pubList, null, 2) }}</pre>
            </div>
            <div class="data-pane">
              <h4 class="wrk-title">Working Data</h4>
              <pre>{{ JSON.stringify(selectedItem?.wrkList, null, 2) }}</pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { ref, computed } from 'vue'
import { axiosWrapper } from '@/utils/axios/axios-wrapper'

export default {
  setup() {
    const projectNo = ref('')
    const keyword = ref('')
    const localSearchQuery = ref('')
    const mergedList = ref([])
    const isLoading = ref(false)
    const selectedIds = ref([])
    const showModal = ref(false)
    const selectedItem = ref(null)

    // 1. 데이터 호출 및 병합 로직 (전체 복구)
    const fetchAndMergeData = async () => {
      if (!keyword.value.trim()) {
        alert('키워드를 입력해주세요.');
        return;
      }
      
      isLoading.value = true;
      selectedIds.value = [];
      const groupMap = new Map();

      try {
        const keywords = keyword.value.split(/[\s,\n]+/).map(k => k.trim()).filter(k => k);
        
        // 병렬 호출
        await Promise.all(keywords.map(async (key) => {
          const payload = { ProjectNo: projectNo.value, Keyword: key };
          
          const [pubRes, wrkRes] = await Promise.all([
            axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload),
            axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload)
          ]);

          const pData = Array.isArray(pubRes?.data) ? pubRes.data : (Array.isArray(pubRes) ? pubRes : []);
          const wData = Array.isArray(wrkRes?.data) ? wrkRes.data : (Array.isArray(wrkRes) ? wrkRes : []);

          pData.forEach(item => {
            if (!groupMap.has(item.TagIdx)) {
              groupMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pubList: [], wrkList: [] });
            }
            groupMap.get(item.TagIdx).pubList.push(item);
          });

          wData.forEach(item => {
            if (!groupMap.has(item.TagIdx)) {
              groupMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pubList: [], wrkList: [] });
            }
            groupMap.get(item.TagIdx).wrkList.push(item);
          });
        }));

        // TagNo 기준 정렬하여 저장
        mergedList.value = Array.from(groupMap.values()).sort((a, b) => 
          String(a.TagNo).localeCompare(String(b.TagNo))
        );

      } catch (error) {
        console.error(error);
        alert('데이터 호출 중 오류가 발생했습니다.');
      } finally {
        isLoading.value = false;
      }
    };

    // 2. 결과 내 실시간 필터링
    const filteredList = computed(() => {
      if (!localSearchQuery.value.trim()) return mergedList.value;
      const query = localSearchQuery.value.toLowerCase();
      return mergedList.value.filter(group => 
        String(group.TagNo).toLowerCase().includes(query) || 
        String(group.TagIdx).includes(query)
      );
    });

    // 3. 텍스트 하이라이트
    const highlightText = (text) => {
      if (!localSearchQuery.value.trim()) return text;
      const query = localSearchQuery.value.trim();
      const regex = new RegExp(`(${query})`, 'gi');
      return String(text).replace(regex, '<mark class="hl">$1</mark>');
    };

    // 4. 모달 제어
    const openModal = (group) => {
      selectedItem.value = group;
      showModal.value = true;
    };
    const closeModal = () => {
      showModal.value = false;
      selectedItem.value = null;
    };

    // 5. 체크박스 제어
    const toggleAllSelect = () => {
      if (selectedIds.value.length === filteredList.value.length) {
        selectedIds.value = [];
      } else {
        selectedIds.value = filteredList.value.map(g => g.TagIdx);
      }
    };

    return {
      projectNo, keyword, localSearchQuery, mergedList, filteredList, isLoading,
      selectedIds, showModal, selectedItem,
      fetchAndMergeData, highlightText, openModal, closeModal, toggleAllSelect,
      isAllSelected: computed(() => 
        filteredList.value.length > 0 && selectedIds.value.length === filteredList.value.length
      ),
      deleteSelected: () => {
        if(confirm(`선택한 ${selectedIds.value.length}건의 그룹을 삭제하시겠습니까?`)) {
          console.log('Delete Target IDs:', selectedIds.value);
        }
      },
      downloadExcel: () => alert('CSV 다운로드 기능을 준비 중입니다.')
    };
  }
}
</script>

<style scoped>
/* CSS는 이전 답변의 '행 높이 최적화' 버전을 그대로 유지합니다. */
.main-layout { height: 100vh; display: flex; flex-direction: column; background: #f1f5f9; }
.dashboard-header { background: white; padding: 20px 30px; border-bottom: 1px solid #cbd5e1; }
.search-grid { display: flex; gap: 30px; align-items: flex-start; }
.project-box { width: 200px; display: flex; flex-direction: column; gap: 15px; }
.filter-input { width: 100%; border: 2px solid #3b82f6; border-radius: 4px; padding: 8px; background: #eff6ff; }
.keyword-box { flex: 1; }
.keyword-input { width: 100%; height: 45px; border: 1px solid #cbd5e1; border-radius: 4px; padding: 10px; transition: height 0.2s; }
.keyword-input:focus { height: 120px; }
.btn-fetch-large { min-width: 240px; height: 45px; background: #1e293b; color: white; border-radius: 4px; font-weight: bold; cursor: pointer; }
.table-container { flex: 1; overflow: auto; margin: 0 30px 20px; background: white; border: 1px solid #cbd5e1; }
.modern-table { width: 100%; border-collapse: separate; border-spacing: 0; }
.cell-content { min-height: 55px; display: flex; align-items: center; padding: 8px 12px; box-sizing: border-box; }
.center { justify-content: center; }
.main-header th { position: sticky; top: 0; z-index: 35; height: 45px; background: #f1f5f9; border-bottom: 1px solid #cbd5e1; border-right: 1px solid #cbd5e1; }
.sub-header th { position: sticky; top: 45px; z-index: 34; height: 40px; background: #f8fafc; border-bottom: 2px solid #94a3b8; border-right: 1px solid #cbd5e1; font-size: 11px; }
.sticky-col { position: sticky; z-index: 40 !important; background: #f8fafc !important; border-right: 1px solid #cbd5e1; }
.col-chk { left: 0; width: 50px; }
.col-idx { left: 50px; width: 70px; }
.col-tag { left: 120px; width: 220px; }
.border-right { border-right: 1px solid #cbd5e1 !important; }
.border-bold-right { border-right: 3px double #94a3b8 !important; }
.action-bar { padding: 10px 30px; display: flex; justify-content: space-between; align-items: center; }
.right-buttons { display: flex; gap: 10px; }
.group-tbody { border-bottom: 4px solid #cbd5e1; }
.cell-pub { background: #f0f9ff; }
.cell-wrk { background: #fffbeb; }
.tag-text { color: #2563eb; font-weight: bold; font-size: 13px; }
.badge { padding: 4px 10px; border-radius: 4px; font-size: 11px; font-weight: bold; color: white; }
.red { background: #ef4444; }
.green { background: #10b981; }
.clickable { cursor: pointer; }
.clickable:hover { filter: brightness(0.96); }

/* 모달 스타일 추가 */
.modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.6); display: flex; align-items: center; justify-content: center; z-index: 1000; }
.modal-content { background: white; width: 1000px; border-radius: 12px; overflow: hidden; }
.modal-header { padding: 20px; border-bottom: 1px solid #eee; display: flex; justify-content: space-between; align-items: center; }
.modal-body { padding: 25px; max-height: 70vh; overflow-y: auto; }
.modal-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.data-pane pre { background: #1e293b; color: #e2e8f0; padding: 15px; border-radius: 8px; font-size: 12px; overflow: auto; }
.btn-close { background: none; border: none; font-size: 24px; cursor: pointer; }

::v-deep .hl { background: #fbbf24; color: black; font-weight: bold; }
</style>
