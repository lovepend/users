=LET(
    TargetHeaders, (Table1[#Headers]="Tag No") + (Table1[#Headers]="Tag Desc"),
    FilteredData, FILTER(Table1, Table1[TRUE/FALSE]=TRUE),
    VSTACK(
        FILTER(Table1[#Headers], TargetHeaders),
        FILTER(FilteredData, TargetHeaders)
    )
)
