<template>
  <div id="app" class="admin-tool">
    <div class="main-layout">
      <header class="dashboard-header">
        <div class="header-container">
          <div class="header-top">
            <h2 class="main-title">Tag Manager <span class="sub-title">v2.0 Auto-Layout</span></h2>
          </div>
          <div class="search-grid">
            <div class="project-box">
              <div class="input-group">
                <label>Project No</label>
                <input v-model="projectNo" type="text" placeholder="P0001" class="base-input unified-height" />
              </div>
              <div class="filter-wrapper">
                <label class="sub-label">결과 내 필터</label>
                <input v-model="localSearchQuery" type="text" placeholder="Tag No 검색..." class="filter-input unified-height" />
              </div>
            </div>
            
            <div class="keyword-box">
              <label>API Fetch Keywords</label>
              <textarea v-model="keyword" placeholder="Enter keywords (Space/Line separated)..." class="base-input"></textarea>
            </div>
            
            <div class="button-box">
              <button @click="fetchAndMergeData" :disabled="isLoading" class="btn-fetch-modern">
                <span v-if="!isLoading">조회하기</span>
                <span v-else class="spinner"></span>
              </button>
            </div>
          </div>
        </div>
      </header>

      <div class="action-bar-wrapper" v-if="mergedList.length > 0">
        <div class="action-bar">
          <div class="stats">조회된 그룹: <b>{{ filteredList.length }}</b>건</div>
          <div class="right-buttons">
            <button @click="deleteSelected" class="btn btn-danger" :disabled="selectedIds.length === 0">🗑 선택 삭제</button>
            <button @click="downloadExcel" class="btn btn-success">📥 CSV 내보내기</button>
          </div>
        </div>
      </div>

      <div class="table-card-wrapper">
        <div class="table-container">
          <table class="modern-table auto-layout">
            <thead>
              <tr class="main-header">
                <th rowspan="2" class="sticky-header sticky-col col-chk">
                  <div class="center-content"><input type="checkbox" :checked="isAllSelected" @change="toggleAllSelect" /></div>
                </th>
                <th rowspan="2" class="sticky-header sticky-col col-idx">Idx</th>
                <th rowspan="2" class="sticky-header sticky-col col-tag">Tag No</th>
                
                <th colspan="3" class="header-group pub-group h-40">Published (Master)</th>
                <th colspan="3" class="header-group wrk-group h-40">Working (Current)</th>
              </tr>
              <tr class="sub-header">
                <th class="h-40">EP ID</th><th class="h-40">CLS</th><th class="h-40">Deleted</th>
                <th class="h-40">EP ID</th><th class="h-40">CLS</th><th class="h-40">Deleted</th>
              </tr>
            </thead>
            
            <tbody v-for="group in filteredList" :key="group.TagIdx" class="group-tbody">
              <tr v-for="(rowNum) in Math.max(group.pubList.length, group.wrkList.length, 1)" :key="group.TagIdx + '-' + rowNum" class="data-row">
                
                <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-chk group-fixed">
                  <div class="cell-content center"><input type="checkbox" :value="group.TagIdx" v-model="selectedIds" /></div>
                </td>
                <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-idx group-fixed clickable" @click="openModal(group)">
                  <div class="cell-content center font-bold text-gray auto-width">{{ group.TagIdx }}</div>
                </td>
                <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-tag group-fixed clickable" @click="openModal(group)">
                  <div class="cell-content tag-text auto-width" v-html="highlightText(group.TagNo)"></div>
                </td>

                <td class="cell-pub clickable" @click="openModal(group)"><div class="cell-content">{{ group.pubList[rowNum-1]?.EpId || '-' }}</div></td>
                <td class="cell-pub clickable" @click="openModal(group)"><div class="cell-content">{{ group.pubList[rowNum-1]?.ClsId || '-' }}</div></td>
                <td class="cell-pub clickable" @click="openModal(group)">
                  <div class="cell-content center">
                    <span v-if="group.pubList[rowNum-1]" :class="['badge-modern', group.pubList[rowNum-1].Deleted ? 'is-true' : 'is-false']">{{ group.pubList[rowNum-1].Deleted }}</span>
                    <span v-else>-</span>
                  </div>
                </td>
                <td class="cell-wrk clickable" @click="openModal(group)"><div class="cell-content">{{ group.wrkList[rowNum-1]?.EpId || '-' }}</div></td>
                <td class="cell-wrk clickable" @click="openModal(group)"><div class="cell-content">{{ group.wrkList[rowNum-1]?.ClsId || '-' }}</div></td>
                <td class="cell-wrk clickable" @click="openModal(group)">
                  <div class="cell-content center">
                    <span v-if="group.wrkList[rowNum-1]" :class="['badge-modern', group.wrkList[rowNum-1].Deleted ? 'is-true' : 'is-false']">{{ group.wrkList[rowNum-1].Deleted }}</span>
                    <span v-else>-</span>
                  </div>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <div v-if="showModal" class="modal-overlay" @click.self="closeModal">
      <div class="modal-content-modern">
        <div class="modal-header-modern">
          <h3>Detail Data Analysis <small>#{{ selectedItem?.TagNo }}</small></h3>
          <button @click="closeModal" class="btn-close-modern">&times;</button>
        </div>
        <div class="modal-body-modern">
          <div class="modal-grid-modern">
            <div class="data-pane"><h4>Published</h4><pre>{{ JSON.stringify(selectedItem?.pubList, null, 2) }}</pre></div>
            <div class="data-pane"><h4>Working</h4><pre>{{ JSON.stringify(selectedItem?.wrkList, null, 2) }}</pre></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { ref, computed } from 'vue'
import { axiosWrapper } from '@/utils/axios/axios-wrapper'

export default {
  setup() {
    const projectNo = ref('')
    const keyword = ref('')
    const localSearchQuery = ref('')
    const mergedList = ref([])
    const isLoading = ref(false)
    const selectedIds = ref([])
    const showModal = ref(false)
    const selectedItem = ref(null)

    // [로직] 데이터 API 호출 및 병합
    const fetchAndMergeData = async () => {
      if (!keyword.value.trim()) return alert('키워드를 입력해주세요.');
      isLoading.value = true;
      selectedIds.value = [];
      const groupMap = new Map();

      try {
        const keywords = keyword.value.split(/[\s,\n]+/).map(k => k.trim()).filter(k => k);
        await Promise.all(keywords.map(async (key) => {
          const payload = { ProjectNo: projectNo.value, Keyword: key };
          const [pubRes, wrkRes] = await Promise.all([
            axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload),
            axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload)
          ]);

          const pData = Array.isArray(pubRes?.data) ? pubRes.data : (Array.isArray(pubRes) ? pubRes : []);
          const wData = Array.isArray(wrkRes?.data) ? wrkRes.data : (Array.isArray(wrkRes) ? wrkRes : []);

          pData.forEach(item => {
            if (!groupMap.has(item.TagIdx)) groupMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pubList: [], wrkList: [] });
            groupMap.get(item.TagIdx).pubList.push(item);
          });
          wData.forEach(item => {
            if (!groupMap.has(item.TagIdx)) groupMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pubList: [], wrkList: [] });
            groupMap.get(item.TagIdx).wrkList.push(item);
          });
        }));
        mergedList.value = Array.from(groupMap.values()).sort((a, b) => String(a.TagNo).localeCompare(String(b.TagNo)));
      } catch (e) { alert('데이터 호출 실패'); } finally { isLoading.value = false; }
    };

    // [로직] 실시간 필터 및 하이라이트
    const filteredList = computed(() => {
      if (!localSearchQuery.value.trim()) return mergedList.value;
      const q = localSearchQuery.value.toLowerCase();
      return mergedList.value.filter(g => String(g.TagNo).toLowerCase().includes(q));
    });

    const highlightText = (text) => {
      if (!localSearchQuery.value.trim()) return text;
      const regex = new RegExp(`(${localSearchQuery.value.trim()})`, 'gi');
      return String(text).replace(regex, '<mark class="hl">$1</mark>');
    };

    // [로직] 체크박스 및 모달 제어
    const toggleAllSelect = () => {
      if (selectedIds.value.length === filteredList.value.length) selectedIds.value = [];
      else selectedIds.value = filteredList.value.map(g => g.TagIdx);
    };

    return {
      projectNo, keyword, localSearchQuery, mergedList, filteredList, isLoading,
      selectedIds, showModal, selectedItem,
      fetchAndMergeData, highlightText, toggleAllSelect,
      openModal: (g) => { selectedItem.value = g; showModal.value = true; },
      closeModal: () => { showModal.value = false; },
      isAllSelected: computed(() => filteredList.value.length > 0 && selectedIds.value.length === filteredList.value.length),
      deleteSelected: () => confirm(`선택한 ${selectedIds.value.length}건을 삭제합니까?`),
      downloadExcel: () => alert('CSV 다운로드 시작')
    };
  }
}
</script>

<style scoped>
/* 1. 레이아웃 & 모던 디자인 배경 */
.main-layout { height: 100vh; display: flex; flex-direction: column; background: #f4f7fa; overflow: hidden; font-family: sans-serif; }
.dashboard-header { background: white; padding: 25px 35px; border-bottom: 1px solid #e2e8f0; flex-shrink: 0; box-shadow: 0 4px 15px rgba(0,0,0,0.03); }
.header-top .main-title { margin: 0 0 15px 0; font-size: 1.5rem; font-weight: 800; color: #1e293b; }
.sub-title { font-size: 0.75rem; background: #e0f2fe; color: #0369a1; padding: 3px 10px; border-radius: 12px; margin-left: 8px; }

.search-grid { display: flex; gap: 25px; align-items: flex-start; }
.project-box { width: 220px; display: flex; flex-direction: column; gap: 10px; }
.unified-height { height: 38px !important; box-sizing: border-box; }
.base-input { border: 1px solid #cbd5e1; border-radius: 6px; padding: 0 12px; font-size: 0.9rem; }
.filter-input { border: 1px solid #3b82f6; background: #eff6ff; }
.keyword-box { flex: 1; }
.keyword-box textarea { width: 100%; height: 45px; border: 1px solid #cbd5e1; border-radius: 6px; padding: 10px; resize: none; transition: height 0.2s; }
.keyword-box textarea:focus { height: 100px; outline: none; border-color: #3b82f6; }
.btn-fetch-modern { min-width: 150px; height: 45px; background: #1e293b; color: white; border-radius: 6px; font-weight: bold; border: none; cursor: pointer; }

/* 2. 가변 폭 및 틀 고정 테이블 설정 */
.table-card-wrapper { flex: 1; overflow: auto; margin: 0 35px 25px; background: white; border-radius: 12px; border: 1px solid #e2e8f0; box-shadow: 0 10px 30px rgba(0,0,0,0.05); }
.table-container { height: 100%; width: 100%; overflow: auto; }
.modern-table.auto-layout { width: 100%; border-collapse: separate; border-spacing: 0; table-layout: auto; } /* 가변 폭의 핵심 */

/* 헤더 높이 및 고정 */
.h-40 { height: 40px !important; padding: 0 !important; }
.main-header th { position: sticky; top: 0; z-index: 50; background: #f8fafc; border-bottom: 1px solid #e2e8f0; border-right: 1px solid #e2e8f0; font-size: 0.8rem; color: #475569; }
.sub-header th { position: sticky; top: 40px; z-index: 49; background: #f8fafc; border-bottom: 2px solid #cbd5e1; border-right: 1px solid #e2e8f0; font-size: 0.75rem; color: #64748b; }

/* 좌측 틀 고정 (Sticky Left) */
.sticky-col { position: sticky; z-index: 40; background: white !important; border-right: 1px solid #e2e8f0; }
.col-chk { left: 0; min-width: 50px; }
.col-idx { left: 50px; min-width: 80px; }
.col-tag { left: 130px; min-width: 200px; }

/* 가변 자릿수 대응: 텍스트가 길어져도 한 줄 유지 */
.auto-width { white-space: nowrap; width: max-content; }

/* 모서리 고정 */
.main-header .sticky-col { z-index: 60 !important; top: 0; }
.sub-header .sticky-col { z-index: 59 !important; top: 40px; }

/* 셀 디자인 */
.cell-content { min-height: 55px; display: flex; align-items: center; padding: 8px 15px; font-size: 0.9rem; }
.center { justify-content: center; width: 100%; }
.tag-text { color: #2563eb; font-weight: 600; font-family: monospace; }
.group-fixed { background: #fcfcfd !important; }
.cell-pub { background: #f0f9ff; }
.cell-wrk { background: #fffdfa; }

/* 배지 & 액션바 */
.badge-modern { padding: 4px 10px; border-radius: 10px; font-size: 0.7rem; color: white; font-weight: bold; }
.is-true { background: #f87171; } .is-false { background: #34d399; }
.action-bar-wrapper { padding: 10px 35px; }
.action-bar { display: flex; justify-content: space-between; align-items: center; padding: 10px 15px; background: white; border-radius: 8px; border: 1px solid #e2e8f0; }
.right-buttons { display: flex; gap: 8px; }
.btn { padding: 8px 15px; border-radius: 4px; border: none; font-size: 0.8rem; cursor: pointer; font-weight: bold; }
.btn-danger { background: #fee2e2; color: #dc2626; }
.btn-success { background: #d1fae5; color: #059669; }

/* 모달 */
.modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.6); backdrop-filter: blur(4px); display: flex; align-items: center; justify-content: center; z-index: 1000; }
.modal-content-modern { background: white; width: 900px; border-radius: 15px; overflow: hidden; }
.modal-header-modern { padding: 20px; border-bottom: 1px solid #eee; display: flex; justify-content: space-between; }
.modal-body-modern { padding: 25px; max-height: 60vh; overflow-y: auto; background: #f9fafb; }
.modal-grid-modern { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.data-pane pre { background: #1e293b; color: #e2e8f0; padding: 15px; border-radius: 8px; font-size: 0.8rem; overflow: auto; }

::v-deep .hl { background: #fbbf24; color: black; font-weight: bold; border-radius: 2px; }
</style>
