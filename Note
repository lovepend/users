 function yft() {
                        return [{
                            label: k.ribbon.review.threadedComments,
                            thumbnailClass: "ribbon-thumbnail-threaded-comments",
                            visibleWhen: `(!${g.TableSheetActive}\n                        && !${g.GanttSheetActive}\n                        && !${g.DataManagerActive}\n                        && !${g.ReportSheetActive})`,
                            commandGroup: {
                                className: "ribbon-group-threaded-comments",
                                commands: [d.NewThreadedComment, d.DeleteThreadedComment, d.PreviousThreadedComment, d.NextThreadedComment, d.ShowThreadedComments]
                            }
                        }]
                    }
