<template>
  <div id="app">
    <h2 class="page-main-title">🔍 태그 통합 조회 시스템 (Pub + Wrk) : Only Test 개발자용 // 추가요청 안받아요</h2>

    <div class="main-content-wrapper">
      <div class="panel full-panel">
        <h3 class="panel-title">키워드 기반 데이터 조회 및 분석</h3>

        <div class="search-controls">
          <div class="input-group-container">
            <div class="input-item">
              <label>Project No</label>
              <input v-model="projectNo" type="text" placeholder="프로젝트 No 입력..." class="custom-input" />
            </div>
            <div class="input-item">
              <label>Keyword</label>
              <input
                v-model="keyword"
                type="text"
                placeholder="태그 번호 및 태그 인덱스 입력..."
                class="custom-input"
                @keyup.enter="fetchAndMergeData"
              />
            </div>
            <button @click="fetchAndMergeData" :disabled="isLoading" class="search-btn">
              {{ isLoading ? '조회 중...' : '통합 검색' }}
            </button>
          </div>
        </div>

        <div class="action-bar" v-if="mergedList.length > 0">
          <div class="filter-group">
            <label>상태 필터:</label>
            <select v-model="filterStatus" class="custom-select">
              <option value="all">전체 보기</option>
              <option value="deleted">삭제된 항목만 (Deleted)</option>
              <option value="active">사용 중인 항목만</option>
            </select>
          </div>
          <button @click="downloadExcel" class="excel-btn">📥 엑셀 다운로드 (CSV)</button>
        </div>

        <div v-if="errorMessage" class="message error-message">⚠️ {{ errorMessage }}</div>

        <div class="table-container" v-if="filteredList.length > 0">
          <table class="data-table">
            <thead>
              <tr>
                <th rowspan="2">Tag Index</th>
                <th rowspan="2">Tag No</th>
                <th colspan="4" class="pub-header">Published (Master)</th>
                <th colspan="4" class="wrk-header">Working (Current)</th>
              </tr>
              <tr>
                <th class="pub-bg">EP ID/IC</th>
                <th class="pub-bg">CLS_ID</th>
                <th class="pub-bg">TypeID</th>
                <th class="pub-bg">Status</th>
                <th class="wrk-bg">EP ID/IC</th>
                <th class="wrk-bg">CLS_ID</th>
                <th class="wrk-bg">DelYN</th>
                <th class="wrk-bg">Status</th>
              </tr>
            </thead>
            <tbody>
              <tr v-for="item in filteredList" :key="item.TagIdx" @click="openModal(item)" class="clickable-row">
                <td class="idx-cell">{{ item.TagIdx }}</td>
                <td class="tag-no-cell">{{ item.TagNo }}</td>

                <td class="pub-bg">{{ item.pub?.EpId || '-' }}</td>
                <td class="pub-bg">{{ item.pub?.ClsId || '-' }}</td>
                <td class="pub-bg">{{ item.pub?.TagTypeId || '-' }}</td>
                <td class="pub-bg">
                  <span v-if="item.pub" :class="item.pub.Deleted ? 'status-red' : 'status-green'">
                    {{ item.pub.Deleted ? 'Deleted' : 'Active' }}
                  </span>
                </td>

                <td class="wrk-bg">{{ item.wrk?.EpId || '-' }}</td>
                <td class="wrk-bg">{{ item.wrk?.ClsId || '-' }}</td>
                <td class="wrk-bg">
                  <span v-if="item.wrk" :class="item.wrk.DelYn ? 'status-red' : ''">{{
                    item.wrk.DelYn ? 'Y' : 'N'
                  }}</span>
                </td>
                <td class="wrk-bg">
                  <span v-if="item.wrk" :class="item.wrk.Deleted ? 'status-red' : 'status-green'">
                    {{ item.wrk.Deleted ? 'Deleted' : 'Active' }}
                  </span>
                </td>
              </tr>
            </tbody>
          </table>
        </div>

        <div v-else-if="searched && !isLoading" class="message no-results-message">데이터가 없습니다.</div>
      </div>
    </div>

    <div v-if="showModal" class="modal-overlay" @click.self="closeModal">
      <div class="modal-content">
        <div class="modal-header">
          <h4>Tag 상세 정보 비교</h4>
          <button @click="closeModal" class="close-btn">&times;</button>
        </div>
        <div class="modal-body" v-if="selectedItem">
          <div class="modal-grid">
            <div class="grid-section">
              <h5>[Master - Published]</h5>
              <pre>{{ JSON.stringify(selectedItem.pub, null, 2) }}</pre>
            </div>
            <div class="grid-section">
              <h5>[Current - Working]</h5>
              <pre>{{ JSON.stringify(selectedItem.wrk, null, 2) }}</pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
  import { ref, computed } from 'vue'
  import { axiosWrapper } from '@/utils/axios/axios-wrapper'

  export default {
    name: 'TagIntegratedManager',
    setup() {
      const projectNo = ref('')
      const keyword = ref('')
      const mergedList = ref([])
      const isLoading = ref(false)
      const errorMessage = ref('')
      const searched = ref(false)

      // 필터 상태
      const filterStatus = ref('all')

      // 모달 상태
      const showModal = ref(false)
      const selectedItem = ref(null)

      // [기능 1] 결과 내 필터링 (Computed)
      const filteredList = computed(() => {
        if (filterStatus.value === 'deleted') {
          return mergedList.value.filter(item => item.pub?.Deleted || item.wrk?.Deleted || item.wrk?.DelYn)
        } else if (filterStatus.value === 'active') {
          return mergedList.value.filter(item => !item.pub?.Deleted && !item.wrk?.Deleted)
        }
        return mergedList.value
      })

      const fetchAndMergeData = async () => {
        if (!keyword.value.trim()) return
        isLoading.value = true
        errorMessage.value = ''
        searched.value = true

        try {
          const payload = { ProjectNo: projectNo.value, Keyword: keyword.value }
          const [pubRes, wrkRes] = await Promise.all([
            axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload),
            axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload),
          ])

          const pubData = pubRes?.data || pubRes
          const wrkData = wrkRes?.data || wrkRes
          const dataMap = new Map()

          if (Array.isArray(pubData)) {
            pubData.forEach(item =>
              dataMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pub: item, wrk: null })
            )
          }
          if (Array.isArray(wrkData)) {
            wrkData.forEach(item => {
              if (dataMap.has(item.TagIdx)) dataMap.get(item.TagIdx).wrk = item
              else dataMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pub: null, wrk: item })
            })
          }
          mergedList.value = Array.from(dataMap.values())
        } catch (err) {
          errorMessage.value = '데이터 호출 실패'
        } finally {
          isLoading.value = false
        }
      }

      // [기능 2] 엑셀(CSV) 다운로드
      const downloadExcel = () => {
        let csvContent = '\uFEFFTagIdx,TagNo,Pub_EpId,Pub_ClsId,Pub_Deleted,Wrk_EpId,Wrk_ClsId,Wrk_DelYn,Wrk_Deleted\n'
        filteredList.value.forEach(item => {
          const row = [
            item.TagIdx,
            item.TagNo,
            item.pub?.EpId || '',
            item.pub?.ClsId || '',
            item.pub?.Deleted || 'false',
            item.wrk?.EpId || '',
            item.wrk?.ClsId || '',
            item.wrk?.DelYn || 'false',
            item.wrk?.Deleted || 'false',
          ]
          csvContent += row.join(',') + '\n'
        })
        const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' })
        const link = document.createElement('a')
        link.href = URL.createObjectURL(blob)
        link.setAttribute('download', `Tag_Export_${new Date().getTime()}.csv`)
        link.click()
      }

      // [기능 3] 모달 제어
      const openModal = item => {
        selectedItem.value = item
        showModal.value = true
      }
      const closeModal = () => {
        showModal.value = false
        selectedItem.value = null
      }

      return {
        projectNo,
        keyword,
        mergedList,
        filteredList,
        isLoading,
        errorMessage,
        searched,
        filterStatus,
        showModal,
        selectedItem,
        fetchAndMergeData,
        downloadExcel,
        openModal,
        closeModal,
      }
    },
  }
</script>

<style scoped>
  #app {
    padding: 20px;
    font-family: sans-serif;
    background: #f0f2f5;
  }
  .page-main-title {
    text-align: center;
    color: #1a73e8;
    margin-bottom: 25px;
  }
  .panel {
    background: white;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
  }

  /* 검색 및 액션바 */
  .search-controls,
  .action-bar {
    display: flex;
    gap: 15px;
    align-items: flex-end;
    margin-bottom: 20px;
    padding: 15px;
    background: #f8f9fa;
    border-radius: 8px;
  }
  .action-bar {
    justify-content: space-between;
    align-items: center;
    border-left: 5px solid #007bff;
  }
  .input-item {
    display: flex;
    flex-direction: column;
    flex: 1;
  }
  .custom-input,
  .custom-select {
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 4px;
    margin-top: 5px;
  }

  .search-btn {
    background: #007bff;
    color: white;
    padding: 10px 25px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    height: 40px;
  }
  .excel-btn {
    background: #28a745;
    color: white;
    padding: 8px 15px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }

  /* 테이블 */
  .table-container {
    overflow-x: auto;
  }
  .data-table {
    width: 100%;
    border-collapse: collapse;
    cursor: pointer;
  }
  .data-table th,
  .data-table td {
    border: 1px solid #eee;
    padding: 12px;
    text-align: center;
  }
  .clickable-row:hover {
    background-color: #f1f8ff !important;
  }

  .pub-header {
    background: #e8f0fe;
    color: #1967d2;
  }
  .wrk-header {
    background: #fef7e0;
    color: #c26401;
  }
  .status-red {
    color: #d93025;
    font-weight: bold;
  }
  .status-green {
    color: #1e8e3e;
    font-weight: bold;
  }

  /* 모달 스타일 */
  .modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
  }
  .modal-content {
    background: white;
    width: 80%;
    max-width: 900px;
    border-radius: 12px;
    padding: 20px;
    max-height: 80vh;
    overflow-y: auto;
  }
  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #eee;
    margin-bottom: 15px;
  }
  .modal-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
  }
  .grid-section pre {
    background: #2d2d2d;
    color: #ccc;
    padding: 15px;
    border-radius: 6px;
    font-size: 0.85em;
    overflow-x: auto;
  }
  .close-btn {
    background: none;
    border: none;
    font-size: 24px;
    cursor: pointer;
  }
</style>
