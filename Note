<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism-tomorrow.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.css" rel="stylesheet" />
    <style>
        :root { --bg-color: #1e1e1e; --header-bg: #2d2d2d; --accent-color: #007acc; }
        body, html { margin: 0; padding: 0; height: 100%; background-color: var(--bg-color); color: #ccc; font-family: 'Consolas', monospace; overflow: hidden; }
        
        .top-bar { height: 35px; background: var(--header-bg); display: flex; justify-content: space-between; align-items: center; padding: 0 15px; border-bottom: 1px solid #3e3e3e; box-sizing: border-box; }
        .logo { font-size: 11px; color: #858585; letter-spacing: 1px; }
        .btn-group { display: flex; gap: 6px; }
        button { background: #3c3c3c; color: #fff; border: 1px solid #454545; padding: 2px 10px; border-radius: 2px; cursor: pointer; font-size: 11px; height: 22px; }
        button#saveBtn { background: var(--accent-color); border: none; }
        button:disabled { opacity: 0.3; }

        .main-container { position: relative; height: calc(100% - 35px); width: 100%; }

        /* 입력 창 스타일 */
        textarea {
            position: absolute; width: 100%; height: 100%; 
            padding: 20px; box-sizing: border-box;
            background: var(--bg-color); color: #d4d4d4;
            font-size: 14px; line-height: 1.6; font-family: inherit;
            border: none; outline: none; resize: none;
            z-index: 2;
            white-space: pre; /* 입력할 때 줄바꿈 유지 */
            overflow: auto;
        }

        /* 보기 창 스타일 */
        #code-display {
            position: absolute; width: 100%; height: 100%;
            z-index: 1; display: none; overflow: auto;
            background: var(--bg-color);
        }

        /* [핵심] Prism 라이브러리 강제 줄바꿈 설정 */
        pre[class*="language-"] {
            margin: 0 !important;
            padding: 20px 20px 20px 3.8em !important; /* 패딩 넉넉히 */
            background: var(--bg-color) !important;
            height: 100% !important;
            box-sizing: border-box !important;
            
            /* 이 3줄이 줄바꿈을 강제합니다 */
            white-space: pre-wrap !important; 
            word-break: break-all !important; 
            overflow-wrap: anywhere !important;
        }

        code[class*="language-"] {
            white-space: pre-wrap !important; /* 코드 태그 줄바꿈 */
            font-family: inherit !important;
        }

        /* 라인 넘버 색상 조정 */
        .line-numbers .line-numbers-rows {
            border-right: 1px solid #444 !important;
            padding-top: 20px !important; /* pre의 패딩과 맞춰야 함 */
        }
    </style>
</head>
<body class="line-numbers">

<div class="top-bar">
    <div class="logo">NOTE</div>
    <div class="btn-group">
        <button id="editBtn">Edit (Ctrl+E)</button>
        <button id="saveBtn" disabled>Save (Ctrl+S)</button>
    </div>
</div>

<div class="main-container">
    <textarea id="editor" spellcheck="false" placeholder="Loading..."></textarea>
    <div id="code-display">
        <pre class="line-numbers"><code id="highlight-area" class="language-javascript"></code></pre>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-javascript.min.js"></script>

<script>
    const editor = document.getElementById('editor');
    const codeDisplay = document.getElementById('code-display');
    const highlightArea = document.getElementById('highlight-area');
    const editBtn = document.getElementById('editBtn');
    const saveBtn = document.getElementById('saveBtn');
    
    const STORAGE_KEY = 'note_v3_final';

    window.onload = async () => {
        const saved = localStorage.getItem(STORAGE_KEY);
        if (saved) {
            editor.value = saved;
        } else {
            try {
                // data.txt가 있을 경우 대비
                const response = await fetch('./data.txt');
                if (response.ok) editor.value = await response.text();
            } catch (e) {
                editor.value = "이곳에 코드를 입력하세요.\n줄바꿈 테스트입니다.";
            }
        }
        setMode(false);
    };

    function updateHighlight() {
        // innerText 대신 textContent를 사용해 줄바꿈을 더 원시적으로 보존합니다.
        highlightArea.textContent = editor.value;
        Prism.highlightElement(highlightArea);
    }

    function setMode(isEdit) {
        if (isEdit) {
            editor.style.display = "block";
            codeDisplay.style.display = "none";
            saveBtn.disabled = false;
            editBtn.textContent = "View";
            editor.focus();
        } else {
            updateHighlight();
            editor.style.display = "none";
            codeDisplay.style.display = "block";
            saveBtn.disabled = true;
            editBtn.textContent = "Edit";
        }
    }

    function save() {
        if (editor.style.display === "none") return;
        localStorage.setItem(STORAGE_KEY, editor.value);
        saveBtn.textContent = "Saved!";
        setTimeout(() => {
            saveBtn.textContent = "Save";
            setMode(false);
        }, 600);
    }

    editBtn.addEventListener('click', () => setMode(editor.style.display === "none"));
    saveBtn.addEventListener('click', save);

    // 단축키
    window.addEventListener('keydown', (e) => {
        const isCtrl = e.ctrlKey || e.metaKey;
        if (isCtrl && e.key.toLowerCase() === 'e') { e.preventDefault(); setMode(editor.style.display === "none"); }
        if (isCtrl && e.key.toLowerCase() === 's') { e.preventDefault(); save(); }
    });

    // 탭 키
    editor.addEventListener('keydown', function(e) {
        if (e.key === 'Tab') {
            e.preventDefault();
            const start = this.selectionStart;
            this.value = this.value.substring(0, start) + "    " + this.value.substring(this.selectionEnd);
            this.selectionStart = this.selectionEnd = start + 4;
        }
    });
</script>
</body>
</html>
