<template>
  <div class="panel panel-expandable m-0 p-0 h-100">
    <PageTitle
      icon="fa-search-plus"
      :title="'Tag 통합 분석 조회' + (manualProjNo ? ` [ ${manualProjNo} ]` : '')"
    >
      <template #actions>
        <div class="d-flex align-items-center gap-3 me-2">
          <label class="d-flex align-items-center gap-2">
            <span class="fw-bold text-primary">Project No</span>
            <input type="text" class="form-control form-control-sm" style="width: 80px;" v-model="manualProjNo" />
          </label>

          <label class="d-flex align-items-center gap-2">
            <span class="fw-bold">조회 키워드</span>
            <textarea 
              class="form-control form-control-sm" 
              style="width: 180px; height: 31px; resize: none;" 
              v-model="searchKeywordRef" 
              placeholder="공백/엔터 구분"
            ></textarea>
          </label>
          <button class="btn btn-xs btn-primary" @click="onSearch">조회</button>

          <div class="ms-3 ps-3 border-start d-flex align-items-center gap-2">
            <span class="fw-bold text-success"><i class="fa-filter"></i> 결과 필터</span>
            <input 
              type="text" 
              class="form-control form-control-sm border-success" 
              style="width: 180px;" 
              v-model="localFilterQuery" 
              placeholder="필터 키워드"
            />
          </div>
        </div>
        <panel-toolbar />
      </template>
    </PageTitle>

    <div class="panel-body m-0 p-0 h-100">
      <div class="panel m-0 p-0 h-100">
        <div class="panel-heading p-0 m-0">
          <div class="w-100 h-100">
            <BasicToolBar
              @delete-data="deleteSelectedData"
              :showCreate="false" :showMultiCreate="false" :showSave="false"
              :showDelete="isSetupAdmin" :isDisabledDeleteBtn="isDisabledDeleteBtn"
            />
          </div>
        </div>
        
        <div class="panel-body overflow-hidden m-0 p-0 h-100">
          <ag-grid-vue 
            v-bind="gridBindProps" 
            @grid-ready="onGridReady"
            @selection-changed="onSelectionChanged"
            @cell-clicked="onCellClicked"
          />
        </div>
      </div>
    </div>

    <div v-if="showModal" class="custom-modal-overlay" @click.self="closeModal">
      <div class="custom-modal-window">
        <div class="modal-header-dark">
          <div class="title-info">
            <span class="badge" :style="{ backgroundColor: selectedRow?.Source === 'Working' ? '#ed6c02' : '#0288d1' }">
              {{ selectedRow?.Source }}
            </span>
            <span class="ms-2 fw-bold">{{ selectedRow?.TagNo }}</span>
          </div>
          <div class="d-flex gap-2">
            <button @click="isJsonMode = !isJsonMode" class="btn btn-xs btn-outline-light">
              {{ isJsonMode ? 'Table View' : 'JSON View' }}
            </button>
            <button @click="closeModal" class="btn btn-xs btn-danger">✕</button>
          </div>
        </div>
        <div class="modal-body-content">
          <pre v-if="isJsonMode" class="json-viewer"><code>{{ JSON.stringify(selectedRow, null, 2) }}</code></pre>
          <div v-else class="table-viewer">
            <table class="detail-analysis-table">
              <thead>
                <tr><th>Property</th><th>Value</th></tr>
              </thead>
              <tbody>
                <tr v-for="(val, key) in displayProperties" :key="key">
                  <td class="prop-key">{{ key }}</td>
                  <td class="prop-val">{{ formatVal(val) }}</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
  import { ref, computed, shallowRef } from 'vue'
  import { storeToRefs } from 'pinia'
  import { useAppPermissionStore } from '@/stores/app-permission'
  import { useAppMenuStore } from '@/stores/app-menu'
  import { axiosWrapper } from '@/utils/axios/axios-wrapper'
  import * as _ from 'lodash-es'

  import AggridLoadingOverlay from '@/components/aggrid/AgGridLoadingOverlay.vue'
  import { sAlert } from '@/utils/messages/alert-common'
  import BasicToolBar from '@/components/toolBar/BasicToolBar.vue'
  import PageTitle from '@/components/toolBar/PageTitle.vue'

  const { isSetupAdmin } = storeToRefs(useAppPermissionStore())
  const appMenu = useAppMenuStore()

  const gridApi = shallowRef()
  const manualProjNo = ref(appMenu.currentProjNo || '') 
  const searchKeywordRef = ref('')
  const localFilterQuery = ref('')
  const rawDataList = ref([])
  const isDisabledDeleteBtn = ref(true)

  // 모달 상태
  const showModal = ref(false)
  const selectedRow = ref(null)
  const isJsonMode = ref(true)

  // --- 1. 다중 필터 및 하이라이트 ---
  const filteredData = computed(() => {
    if (!localFilterQuery.value.trim()) return rawDataList.value
    const kws = localFilterQuery.value.split(/[\s,\n]+/).filter(k => k).map(k => k.toLowerCase())
    return rawDataList.value.filter(i => {
      const target = `${i.TagNo} ${i.EpId} ${i.ClsId} ${i.TagDesc}`.toLowerCase()
      return kws.some(kw => target.includes(kw))
    })
  })

  const highlightRenderer = (params) => {
    if (!params.value || !localFilterQuery.value.trim()) return params.value
    const kws = localFilterQuery.value.split(/[\s,\n]+/).filter(k => k)
    let text = String(params.value)
    kws.forEach(kw => {
      text = text.replace(new RegExp(`(${kw})`, 'gi'), `<mark style="background:#ffeb3b;color:black;">$1</mark>`)
    })
    return text
  }

  // --- 2. 데이터 조회 ---
  const onSearch = () => getData()
  const getData = async () => {
    if (!manualProjNo.value) return sAlert.Warning(`Project No 입력 필요`)
    gridApi.value.setGridOption('loading', true)
    const searchKeys = searchKeywordRef.value.split(/[\s,\n]+/).filter(k => k)
    let combined = []
    try {
      for (const key of searchKeys) {
        const payload = { ProjectNo: manualProjNo.value.trim(), Keyword: key }
        const [p, w] = await Promise.allSettled([
          axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload),
          axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload)
        ])
        if (p.status === 'fulfilled' && p.value) combined.push(...p.value.map(i => ({...i, Source: 'Published'})))
        if (w.status === 'fulfilled' && w.value) combined.push(...w.value.map(i => ({...i, Source: 'Working'})))
      }
      rawDataList.value = _.uniqBy(combined.filter(i => !i.DELETED), (i) => `${i.TagIdx}_${i.Source}`)
    } finally { gridApi.value.setGridOption('loading', false) }
  }

  // --- 3. 모달 제어 ---
  const onCellClicked = (params) => {
    if (params.column.getColId() === 'Source') {
      selectedRow.value = params.data
      showModal.value = true
    }
  }
  const closeModal = () => { showModal.value = false; selectedRow.value = null; }
  const displayProperties = computed(() => _.omit(selectedRow.value, ['Source']))
  const formatVal = (v) => typeof v === 'object' ? JSON.stringify(v) : v

  // --- 4. AG Grid 설정 ---
  const colDefs = [
    {
      headerName: 'Source', field: 'Source', minWidth: 100, pinned: 'left',
      cellStyle: { cursor: 'pointer', fontWeight: 'bold' },
      cellRenderer: p => {
        const color = p.value === 'Working' ? '#ed6c02' : '#0288d1'
        return `<span class="badge" style="background-color:${color};color:white;padding:2px 6px;">${p.value}</span>`
      }
    },
    { field: 'TagIdx', headerName: 'TagIdx', minWidth: 100, pinned: 'left' },
    { field: 'TagNo', headerName: 'TagNo', minWidth: 160, pinned: 'left', cellRenderer: highlightRenderer },
    { field: 'EpId', headerName: 'EP ID', minWidth: 100, cellRenderer: highlightRenderer },
    { field: 'ClsId', headerName: 'Class', minWidth: 100, cellRenderer: highlightRenderer },
    { field: 'TagDesc', headerName: 'Description', minWidth: 300, flex: 1, cellRenderer: highlightRenderer }
  ]

  const gridBindProps = computed(() => ({
    columnDefs: colDefs,
    rowData: filteredData.value,
    style: 'width: 100%; height: 100%',
    class: 'ag-theme-custom',
    pagination: true,
    paginationPageSize: 100,
    loadingOverlayComponent: AggridLoadingOverlay,
    defaultColDef: { sortable: true, filter: true, resizable: true, floatingFilter: true, cellStyle: { background: '#fff' } }
  }))

  const onGridReady = p => { gridApi.value = p.api }
  const onSelectionChanged = () => { isDisabledDeleteBtn.value = gridApi.value.getSelectedNodes().length === 0 }
  const deleteSelectedData = async () => { /* 삭제 로직 */ }

  defineOptions({ name: 'PubWrkFinalTool' })
</script>

<style scoped lang="scss">
  /* 모달 스타일 */
  .custom-modal-overlay {
    position: fixed; top: 0; left: 0; width: 100vw; height: 100vh;
    background: rgba(0,0,0,0.7); z-index: 9999; display: flex; align-items: center; justify-content: center;
  }
  .custom-modal-window {
    background: #fff; width: 80%; height: 80%; border-radius: 8px; display: flex; flex-direction: column; overflow: hidden;
  }
  .modal-header-dark {
    background: #212529; color: #fff; padding: 12px 20px; display: flex; justify-content: space-between; align-items: center;
  }
  .modal-body-content { flex: 1; overflow: auto; padding: 20px; background: #f8f9fa; }
  .json-viewer { background: #2d2d2d; color: #ccc; padding: 15px; border-radius: 4px; font-size: 12px; }
  
  /* 상세 테이블 스타일 */
  .detail-analysis-table {
    width: 100%; border-collapse: collapse; background: #fff;
    th, td { border: 1px solid #dee2e6; padding: 8px 12px; font-size: 13px; }
    th { background: #e9ecef; text-align: left; }
    .prop-key { background: #f1f3f5; font-weight: bold; width: 250px; }
  }

  ::v-deep(.ag-pinned-left-cols-container) { border-right: 2px solid #ddd !important; background: #fff; }
</style>
