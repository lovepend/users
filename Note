<template>
  <div id="app" class="admin-tool">
    <div class="main-layout">
      <header class="dashboard-header">
        <div class="search-grid">
          <div class="input-box proj-box">
            <label class="field-label">Project No</label>
            <input v-model="projectNo" type="text" class="design-input" placeholder="P0001" />
          </div>
          <div class="input-box kw-box">
            <label class="field-label">API Keywords (Space/Comma/Enter)</label>
            <textarea v-model="keyword" class="design-textarea" placeholder="조회 키워드를 입력하세요..."></textarea>
          </div>
          <div class="input-box filter-box">
            <label class="field-label">Result Filter</label>
            <input v-model="localSearchQuery" type="text" class="design-input filter-highlight" placeholder="Tag No 필터..." />
          </div>
          <div class="btn-box">
            <button @click="fetchAndMergeData" :disabled="isLoading" class="primary-gradient-btn">
              {{ isLoading ? '조회 중...' : '데이터 통합 조회' }}
            </button>
          </div>
        </div>
      </header>

      <div class="action-bar" v-if="mergedList.length > 0">
        <div class="result-count">Total Groups: <strong>{{ filteredList.length }}</strong></div>
        <div class="button-group">
          <button @click="deleteSelected" class="btn-action btn-danger-custom" :disabled="selectedIds.length === 0">🗑 선택 삭제</button>
          <button @click="downloadExcel" class="btn-action btn-success-custom">📥 CSV 내보내기</button>
        </div>
      </div>

      <div class="table-outer-container">
        <table class="master-table">
          <thead>
            <tr>
              <th class="sticky-header sticky-col col-chk">Chk</th>
              <th class="sticky-header sticky-col col-idx">Idx</th>
              <th class="sticky-header sticky-col col-tag">Tag No</th>
              <th class="sticky-header col-src">Src</th>
              <th class="sticky-header">EP ID</th>
              <th class="sticky-header">Class ID</th>
              <th class="sticky-header">Status</th>
              <th class="sticky-header">Last Updated</th>
            </tr>
          </thead>
          
          <tbody v-for="group in filteredList" :key="group.TagIdx" class="data-group-body">
            <tr v-for="(item, pIdx) in group.pubList" :key="'p-'+group.TagIdx+'-'+pIdx" class="row-pub">
              <td v-if="pIdx === 0" :rowspan="group.pubList.length + group.wrkList.length" class="sticky-col col-chk group-cell">
                <input type="checkbox" :value="group.TagIdx" v-model="selectedIds" />
              </td>
              <td v-if="pIdx === 0" :rowspan="group.pubList.length + group.wrkList.length" class="sticky-col col-idx group-cell clickable" @click="openModal(group)">
                <div class="idx-val">{{ group.TagIdx }}</div>
              </td>
              <td v-if="pIdx === 0" :rowspan="group.pubList.length + group.wrkList.length" class="sticky-col col-tag group-cell clickable" @click="openModal(group)">
                <div class="tag-val">{{ group.TagNo }}</div>
              </td>
              <td class="col-src"><span class="m-badge pub">P</span></td>
              <td class="clickable" @click="openModal(group)">{{ item.EpId }}</td>
              <td class="clickable" @click="openModal(group)">{{ item.ClsId }}</td>
              <td class="center-vh">
                <span :class="['status-badge', item.Deleted ? 'is-del' : 'is-active']">{{ item.Deleted ? 'Del' : 'Live' }}</span>
              </td>
              <td class="date-txt">{{ item.UpdatedDate || '-' }}</td>
            </tr>
            
            <tr v-for="(item, wIdx) in group.wrkList" :key="'w-'+group.TagIdx+'-'+wIdx" class="row-wrk">
              <td class="col-src"><span class="m-badge wrk">W</span></td>
              <td class="clickable" @click="openModal(group)">{{ item.EpId }}</td>
              <td class="clickable" @click="openModal(group)">{{ item.ClsId }}</td>
              <td class="center-vh">
                <span :class="['status-badge', item.Deleted ? 'is-del' : 'is-active']">{{ item.Deleted ? 'Del' : 'Live' }}</span>
              </td>
              <td class="date-txt">{{ item.UpdatedDate || '-' }}</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <div v-if="showModal" class="modal-overlay" @click.self="closeModal">
      <div class="modal-window">
        <div class="modal-header">
          <div class="title-area">
            <div class="main-title">📊 Full Data Comparison Analysis</div>
            <div class="meta-info-row">
              <span class="info-chip tag-main"><strong>TAG:</strong> {{ selectedItem?.TagNo }}</span>
              <span class="info-chip"><strong>IDX:</strong> {{ selectedItem?.TagIdx }}</span>
            </div>
          </div>
          <div class="modal-actions">
            <button @click="isJsonMode = !isJsonMode" class="btn-mode-toggle">{{ isJsonMode ? '📋 Table' : '📄 JSON' }}</button>
            <button @click="closeModal" class="btn-modal-close">✕</button>
          </div>
        </div>
        <div class="modal-body">
          <div v-if="!isJsonMode" class="modal-table-container">
            <table class="analysis-table auto-layout">
              <thead>
                <tr>
                  <th class="m-sticky-header top-row source-col">Source</th>
                  <th v-for="key in allSystemKeys" :key="'sh-'+key" class="m-sticky-header top-row sys-h">{{ key }}</th>
                  <th v-for="key in allAttributeKeys" :key="'ah-'+key" class="m-sticky-header top-row attr-h">{{ key }}</th>
                </tr>
              </thead>
              <tbody>
                <tr v-for="(item, idx) in selectedItem?.pubList" :key="'p-det-'+idx" class="row-pub">
                  <td class="source-cell"><strong>PUBLISHED</strong></td>
                  <td v-for="key in allSystemKeys" :key="'ps-'+key">{{ item[key] ?? '-' }}</td>
                  <td v-for="key in allAttributeKeys" :key="'pa-'+key">{{ getAttrValue(item, key) }}</td>
                </tr>
                <tr v-for="(item, idx) in selectedItem?.wrkList" :key="'w-det-'+idx" class="row-wrk">
                  <td class="source-cell"><strong>WORKING</strong></td>
                  <td v-for="key in allSystemKeys" :key="'ws-'+key">{{ item[key] ?? '-' }}</td>
                  <td v-for="key in allAttributeKeys" :key="'wa-'+key">{{ getAttrValue(item, key) }}</td>
                </tr>
              </tbody>
            </table>
          </div>
          <div v-else class="json-viewer-container">
             <div class="json-pane"><h3>Published</h3><pre><code>{{ JSON.stringify(selectedItem?.pubList, null, 2) }}</code></pre></div>
             <div class="json-pane"><h3>Working</h3><pre><code>{{ JSON.stringify(selectedItem?.wrkList, null, 2) }}</code></pre></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { ref, computed } from 'vue'
import { axiosWrapper } from '@/utils/axios/axios-wrapper'

export default {
  setup() {
    const projectNo = ref('')
    const keyword = ref('')
    const localSearchQuery = ref('')
    const mergedList = ref([])
    const isLoading = ref(false)
    const selectedIds = ref([])
    const showModal = ref(false)
    const selectedItem = ref(null)
    const isJsonMode = ref(false)

    const filteredList = computed(() => {
      const q = localSearchQuery.value.trim().toLowerCase();
      if (!q) return mergedList.value;
      return mergedList.value.filter(g => g.TagNo.toLowerCase().includes(q));
    });

    const fetchAndMergeData = async () => {
      if (!keyword.value.trim()) return alert('키워드 필수!');
      isLoading.value = true;
      const groupMap = new Map();
      try {
        const keys = keyword.value.split(/[\s,]+/).filter(k => k);
        for (const k of keys) {
          const payload = { ProjectNo: projectNo.value, Keyword: k };
          const [p, w] = await Promise.all([
            axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload).catch(() => []),
            axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload).catch(() => [])
          ]);
          [...p, ...w].forEach(i => {
            if (!groupMap.has(i.TagIdx)) groupMap.set(i.TagIdx, { TagIdx: i.TagIdx, TagNo: i.TagNo, pubList: [], wrkList: [] });
            const targetList = p.includes(i) ? groupMap.get(i.TagIdx).pubList : groupMap.get(i.TagIdx).wrkList;
            targetList.push(i);
          });
        }
        mergedList.value = Array.from(groupMap.values()).sort((a,b) => a.TagNo.localeCompare(b.TagNo));
      } finally { isLoading.value = false; }
    };

    const allSystemKeys = computed(() => {
      if (!selectedItem.value) return [];
      const keys = new Set();
      const ignore = ['Attributes', 'attributes', 'TagNo', 'TagIdx', '_id', 'UpdatedDate'];
      [...selectedItem.value.pubList, ...selectedItem.value.wrkList].forEach(i => {
        Object.keys(i).forEach(k => { if(!ignore.includes(k)) keys.add(k); });
      });
      return Array.from(keys).sort();
    });

    const allAttributeKeys = computed(() => {
      if (!selectedItem.value) return [];
      const keys = new Set();
      [...selectedItem.value.pubList, ...selectedItem.value.wrkList].forEach(i => {
        (i.Attributes || i.attributes || []).forEach(a => keys.add(a.ATT_ID));
      });
      return Array.from(keys).sort();
    });

    const getAttrValue = (item, attId) => {
      const a = (item.Attributes || item.attributes || []).find(x => x.ATT_ID === attId);
      return a ? (a.VALUE ?? a.VALUE_DBL ?? '-') : '-';
    };

    return {
      projectNo, keyword, localSearchQuery, mergedList, filteredList, isLoading, selectedIds, showModal, selectedItem, isJsonMode,
      allSystemKeys, allAttributeKeys, getAttrValue, fetchAndMergeData,
      openModal: (g) => { selectedItem.value = g; showModal.value = true; },
      closeModal: () => { showModal.value = false; },
      toggleAllSelect: () => { selectedIds.value = selectedIds.value.length === filteredList.value.length ? [] : filteredList.value.map(g => g.TagIdx) },
      isAllSelected: computed(() => filteredList.value.length > 0 && selectedIds.value.length === filteredList.value.length),
      deleteSelected: () => {}, downloadExcel: () => {}
    };
  }
}
</script>

<style scoped>
/* [1] 레이아웃 및 한 줄 고정 헤더 */
.admin-tool { font-family: 'Inter', sans-serif; background: #f8fafc; color: #1e293b; }
.main-layout { height: 100vh; display: flex; flex-direction: column; overflow: hidden; }

.dashboard-header { background: #fff; padding: 20px 30px; border-bottom: 1px solid #e2e8f0; }
.search-grid { 
  display: flex; gap: 15px; align-items: flex-end; flex-wrap: nowrap; /* 밀림 방지 핵심 */
}
.proj-box { width: 130px; flex-shrink: 0; }
.kw-box { flex: 2; min-width: 300px; }
.filter-box { flex: 1; min-width: 200px; }
.btn-box { flex-shrink: 0; }

.design-input, .design-textarea { width: 100%; border: 1px solid #cbd5e1; border-radius: 8px; padding: 10px; font-size: 13px; }
.design-textarea { height: 42px; resize: none; }
.primary-gradient-btn { background: #2563eb; color: #fff; border: none; padding: 0 25px; border-radius: 8px; font-weight: 700; height: 42px; cursor: pointer; }

/* [2] 메인 테이블 수직 나열 구조 */
.table-outer-container { flex: 1; overflow: auto; margin: 15px 30px; border: 1px solid #e2e8f0; border-radius: 12px; background: #fff; }
.master-table { width: 100%; border-collapse: separate; border-spacing: 0; }
.master-table th { position: sticky; top: 0; z-index: 100; background: #f1f5f9; border-bottom: 2px solid #cbd5e1; border-right: 1px solid #e2e8f0; padding: 12px; font-size: 11px; text-transform: uppercase; color: #64748b; }

/* 좌측 고정 컬럼 */
.sticky-col { position: sticky; left: 0; z-index: 110; background: #fff !important; border-right: 1px solid #e2e8f0; }
.col-chk { width: 50px; text-align: center; }
.col-idx { width: 120px; left: 50px; text-align: center; }
.col-tag { width: 220px; left: 170px; border-right: 4px double #cbd5e1 !important; padding: 0 15px; }

.group-cell { border-bottom: 4px solid #cbd5e1 !important; vertical-align: middle; }
.row-pub { background: #f0f7ff; } /* Published 행 하늘색 */
.row-wrk { background: #ffffff; } /* Working 행 흰색 */

/* 출처 배지 */
.col-src { width: 60px; text-align: center; }
.m-badge { display: inline-block; width: 22px; height: 22px; line-height: 22px; border-radius: 4px; font-size: 11px; font-weight: 900; color: #fff; }
.m-badge.pub { background: #3b82f6; }
.m-badge.wrk { background: #f97316; }

/* [3] 모달 스타일 */
.modal-overlay { position: fixed; inset: 0; background: rgba(15,23,42,0.8); display: flex; align-items: center; justify-content: center; z-index: 2000; }
.modal-window { background: #fff; width: 95%; height: 90vh; border-radius: 20px; display: flex; flex-direction: column; overflow: hidden; }
.modal-header { padding: 20px 30px; background: #0f172a; color: #fff; display: flex; justify-content: space-between; }
.tag-main { background: #3b82f6 !important; color: #fff !important; }

.modal-body { flex: 1; overflow: hidden; padding: 25px; background: #f8fafc; }
.modal-table-container { flex: 1; overflow: auto; border: 1px solid #e2e8f0; background: #fff; border-radius: 8px; }

.analysis-table.auto-layout { width: max-content; min-width: 100%; border-collapse: separate; }
.analysis-table th { position: sticky; top: 0; z-index: 10; background: #f1f5f9; padding: 12px 20px; border-bottom: 2px solid #cbd5e1; border-right: 1px solid #e2e8f0; font-size: 12px; }
.source-col { position: sticky; left: 0; z-index: 20; background: #f1f5f9 !important; border-right: 3px double #cbd5e1 !important; }
.source-cell { position: sticky; left: 0; background: inherit; border-right: 3px double #cbd5e1 !important; text-align: center; font-size: 11px; }

.status-badge { padding: 3px 8px; border-radius: 6px; font-size: 11px; font-weight: 700; color: #fff; }
.is-active { background: #10b981; } .is-del { background: #ef4444; }
.clickable { cursor: pointer; } .clickable:hover { text-decoration: underline; color: #2563eb; }
.date-txt { font-size: 11px; color: #94a3b8; }
</style>
