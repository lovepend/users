<template>
  <div id="app" class="admin-tool">
    <div class="main-layout">
      <header class="dashboard-header">
        <div class="search-grid">
          <div class="project-box">
            <div class="input-group">
              <label>Project No</label>
              <input v-model="projectNo" type="text" class="base-input unified-height" placeholder="P0001" />
            </div>
            <div class="filter-wrapper">
              <label class="sub-label">결과 내 실시간 필터</label>
              <input v-model="localSearchQuery" type="text" class="filter-input unified-height" placeholder="Tag No 검색..." />
            </div>
          </div>
          <div class="keyword-box">
            <label>API Fetch Keywords</label>
            <textarea v-model="keyword" class="base-input keyword-input" placeholder="키워드를 입력하세요 (공백/엔터 구분)"></textarea>
          </div>
          <div class="button-box">
            <button @click="fetchAndMergeData" :disabled="isLoading" class="btn-fetch-large">
              {{ isLoading ? '조회 중...' : '데이터 통합 조회' }}
            </button>
          </div>
        </div>
      </header>

      <div class="action-bar" v-if="mergedList.length > 0">
        <div class="stats">조회 결과: <b>{{ filteredList.length }}</b>건</div>
        <div class="right-buttons">
          <button @click="deleteSelected" class="btn btn-danger" :disabled="selectedIds.length === 0">🗑 선택 삭제</button>
          <button @click="downloadExcel" class="btn btn-success">📥 CSV 내보내기</button>
        </div>
      </div>

      <div class="table-container">
        <table class="modern-table auto-layout">
          <thead>
            <tr class="main-header">
              <th rowspan="2" class="sticky-header sticky-col col-chk">
                <div class="center-vh"><input type="checkbox" :checked="isAllSelected" @change="toggleAllSelect" /></div>
              </th>
              <th rowspan="2" class="sticky-header sticky-col col-idx">Idx</th>
              <th rowspan="2" class="sticky-header sticky-col col-tag">Tag No</th>
              <th colspan="3" class="pub-header-top">Published (Master)</th>
              <th colspan="3" class="wrk-header-top">Working (Current)</th>
            </tr>
            <tr class="sub-header">
              <th class="sub-th">EP ID</th><th class="sub-th">CLS</th><th class="sub-th">Del</th>
              <th class="sub-th">EP ID</th><th class="sub-th">CLS</th><th class="sub-th">Del</th>
            </tr>
          </thead>
          
          <tbody v-for="group in filteredList" :key="group.TagIdx" class="group-tbody">
            <tr v-for="(rowNum) in Math.max(group.pubList.length, group.wrkList.length, 1)" :key="group.TagIdx + '-' + rowNum">
              <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-chk group-fixed">
                <div class="center-vh"><input type="checkbox" :value="group.TagIdx" v-model="selectedIds" /></div>
              </td>
              <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-idx group-fixed clickable" @click="openModal(group)">
                <div class="center-vh font-bold">{{ group.TagIdx }}</div>
              </td>
              <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-tag group-fixed clickable" @click="openModal(group)">
                <div class="cell-content tag-text" v-html="highlightText(group.TagNo)"></div>
              </td>
              <td class="cell-pub clickable" @click="openModal(group)"><div class="cell-content">{{ group.pubList[rowNum-1]?.EpId || '-' }}</div></td>
              <td class="cell-pub clickable" @click="openModal(group)"><div class="cell-content">{{ group.pubList[rowNum-1]?.ClsId || '-' }}</div></td>
              <td class="cell-pub clickable" @click="openModal(group)">
                <div class="cell-content center-vh">
                  <span v-if="group.pubList[rowNum-1]" :class="['badge', group.pubList[rowNum-1].Deleted ? 'red' : 'green']">{{ group.pubList[rowNum-1].Deleted ? 'Y' : 'N' }}</span>
                </div>
              </td>
              <td class="cell-wrk clickable" @click="openModal(group)"><div class="cell-content">{{ group.wrkList[rowNum-1]?.EpId || '-' }}</div></td>
              <td class="cell-wrk clickable" @click="openModal(group)"><div class="cell-content">{{ group.wrkList[rowNum-1]?.ClsId || '-' }}</div></td>
              <td class="cell-wrk clickable" @click="openModal(group)">
                <div class="cell-content center-vh">
                  <span v-if="group.wrkList[rowNum-1]" :class="['badge', group.wrkList[rowNum-1].Deleted ? 'red' : 'green']">{{ group.wrkList[rowNum-1].Deleted ? 'Y' : 'N' }}</span>
                </div>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <div v-if="showModal" class="modal-overlay" @click.self="closeModal">
      <div class="modal-content modal-xl">
        <div class="modal-header">
          <div class="header-left">
            <h3>상세 데이터 비교 분석</h3>
            <span class="tag-label">{{ selectedItem?.TagNo }}</span>
          </div>
          <div class="header-right">
            <button @click="isJsonMode = !isJsonMode" class="btn-mode">
              {{ isJsonMode ? '📊 테이블 뷰' : '{} JSON 원본' }}
            </button>
            <button @click="closeModal" class="btn-close">&times;</button>
          </div>
        </div>

        <div class="modal-body modal-scroll">
          <div v-if="!isJsonMode" class="modal-table-wrapper">
            <table class="analysis-table dynamic-width">
              <thead>
                <tr>
                  <th colspan="5" class="h-info">시스템 기본정보 (System Info)</th>
                  <th :colspan="allAttributeKeys.length" class="h-attr">특성 정보 (Attributes)</th>
                </tr>
                <tr>
                  <th class="min-w-100">Source</th>
                  <th class="min-w-150">Tag No</th>
                  <th class="min-w-120">EP ID</th>
                  <th class="min-w-120">Class</th>
                  <th class="min-w-80">상태</th>
                  <th v-for="key in allAttributeKeys" :key="key" class="attr-head">{{ key }}</th>
                </tr>
              </thead>
              <tbody>
                <tr v-for="(row, idx) in modalRows" :key="idx" :class="row.source">
                  <td><span :class="['label', row.source]">{{ row.source }}</span></td>
                  <td class="font-bold text-blue">{{ row.TagNo }}</td>
                  <td>{{ row.EpId }}</td>
                  <td>{{ row.ClsId }}</td>
                  <td class="text-center">
                    <span :class="['badge', row.Deleted ? 'red' : 'green']">
                      {{ row.Deleted ? 'Deleted' : 'Active' }}
                    </span>
                  </td>
                  <td v-for="key in allAttributeKeys" :key="key + idx" :class="{ 'empty': row.attrMap[key] === '-' }">
                    {{ row.attrMap[key] }}
                  </td>
                </tr>
              </tbody>
            </table>
          </div>

          <div v-else class="json-viewer">
            <div class="json-pane">
              <h5>Published Raw</h5>
              <pre>{{ JSON.stringify(selectedItem?.pubList, null, 2) }}</pre>
            </div>
            <div class="json-pane">
              <h5>Working Raw</h5>
              <pre>{{ JSON.stringify(selectedItem?.wrkList, null, 2) }}</pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { ref, computed } from 'vue'
import { axiosWrapper } from '@/utils/axios/axios-wrapper'

export default {
  setup() {
    const projectNo = ref('')
    const keyword = ref('')
    const localSearchQuery = ref('')
    const mergedList = ref([])
    const isLoading = ref(false)
    const selectedIds = ref([])
    const showModal = ref(false)
    const selectedItem = ref(null)
    const isJsonMode = ref(false)

    // 데이터 조회 및 병합 로직
    const fetchAndMergeData = async () => {
      if (!keyword.value.trim()) return alert('키워드를 입력하세요.');
      isLoading.value = true;
      const groupMap = new Map();
      try {
        const keys = keyword.value.split(/[\s,\n]+/).map(k => k.trim()).filter(k => k);
        await Promise.all(keys.map(async (k) => {
          const payload = { ProjectNo: projectNo.value, Keyword: k };
          const [pubRes, wrkRes] = await Promise.all([
            axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload),
            axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload)
          ]);
          const pList = pubRes?.data || pubRes || [];
          const wList = wrkRes?.data || wrkRes || [];
          const group = (list, type) => {
            list.forEach(i => {
              if(!groupMap.has(i.TagIdx)) groupMap.set(i.TagIdx, { TagIdx: i.TagIdx, TagNo: i.TagNo, pubList: [], wrkList: [] });
              groupMap.get(i.TagIdx)[type].push(i);
            });
          };
          group(pList, 'pubList'); group(wList, 'wrkList');
        }));
        mergedList.value = Array.from(groupMap.values()).sort((a,b) => String(a.TagNo).localeCompare(b.TagNo));
      } catch(e) { console.error(e); }
      finally { isLoading.value = false; }
    };

    // 모달용 Attributes 합집합 헤더 추출
    const allAttributeKeys = computed(() => {
      if (!selectedItem.value) return [];
      const keys = new Set();
      const gather = (list) => list.forEach(row => {
        const attrs = row.Attributes || row.attributes || [];
        if (Array.isArray(attrs)) {
          attrs.forEach(attr => { if(attr.ATT_ID) keys.add(attr.ATT_ID); });
        }
      });
      gather(selectedItem.value.pubList);
      gather(selectedItem.value.wrkList);
      return Array.from(keys).sort();
    });

    // 모달용 데이터 행 가공 (TagNo, Deleted 보강)
    const modalRows = computed(() => {
      if (!selectedItem.value) return [];
      const process = (item, src) => {
        const attrMap = {};
        const rawAttrs = item.Attributes || item.attributes || [];
        if (Array.isArray(rawAttrs)) {
          rawAttrs.forEach(a => {
            if (a.ATT_ID) {
              let val = '-';
              if (a.VALUE !== null && a.VALUE !== undefined && String(a.VALUE).trim() !== '') {
                val = String(a.VALUE);
              } else if (a.VALUE_DBL !== null && a.VALUE_DBL !== 0) {
                val = String(a.VALUE_DBL);
              }
              attrMap[a.ATT_ID] = val;
            }
          });
        }
        return {
          source: src,
          TagNo: item.TagNo || selectedItem.value.TagNo,
          EpId: item.EpId || '-',
          ClsId: item.ClsId || item.Cls || '-',
          Deleted: !!item.Deleted,
          attrMap
        };
      };
      const result = [];
      selectedItem.value.pubList.forEach(i => result.push(process(i, 'Published')));
      selectedItem.value.wrkList.forEach(i => result.push(process(i, 'Working')));
      return result;
    });

    const filteredList = computed(() => {
      const q = localSearchQuery.value.trim().toLowerCase();
      return q ? mergedList.value.filter(g => String(g.TagNo).toLowerCase().includes(q)) : mergedList.value;
    });

    return {
      projectNo, keyword, localSearchQuery, mergedList, filteredList, isLoading, selectedIds, showModal, selectedItem, isJsonMode,
      allAttributeKeys, modalRows, fetchAndMergeData,
      highlightText: (t) => {
        const q = localSearchQuery.value.trim();
        return q ? String(t).replace(new RegExp(`(${q})`, 'gi'), '<mark class="hl">$1</mark>') : t;
      },
      openModal: (g) => { selectedItem.value = g; showModal.value = true; isJsonMode.value = false; },
      closeModal: () => { showModal.value = false; },
      toggleAllSelect: () => {
        selectedIds.value = (selectedIds.value.length === filteredList.value.length) ? [] : filteredList.value.map(g => g.TagIdx);
      },
      isAllSelected: computed(() => filteredList.value.length > 0 && selectedIds.value.length === filteredList.value.length),
      deleteSelected: () => confirm('삭제하시겠습니까?'),
      downloadExcel: () => alert('CSV 다운로드 시작')
    };
  }
}
</script>

<style scoped>
/* 1. 기본 레이아웃 */
.main-layout { height: 100vh; display: flex; flex-direction: column; background: #f1f5f9; overflow: hidden; }
.dashboard-header { background: white; padding: 20px 30px; border-bottom: 1px solid #cbd5e1; flex-shrink: 0; }
.search-grid { display: flex; gap: 20px; align-items: flex-start; }
.project-box { width: 220px; display: flex; flex-direction: column; gap: 8px; }
.unified-height { height: 36px !important; border: 1px solid #cbd5e1; border-radius: 4px; padding: 0 10px; font-size: 13px; }
.filter-input { border: 2px solid #3b82f6; background: #eff6ff; }
.keyword-box { flex: 1; }
.keyword-input { width: 100%; height: 45px; border: 1px solid #cbd5e1; border-radius: 4px; padding: 10px; resize: none; font-size: 13px; }
.btn-fetch-large { min-width: 160px; height: 45px; background: #1e293b; color: white; border-radius: 4px; font-weight: bold; cursor: pointer; border: none; }

/* 2. 메인 테이블 (헤더 공백 제거용 스타일) */
.table-container { flex: 1; overflow: auto; margin: 0 30px 20px; background: white; border: 1px solid #cbd5e1; }
.modern-table { width: 100%; border-collapse: separate; border-spacing: 0; table-layout: fixed; }

/* 헤더 상하 공백 제거를 위한 고정 높이 및 수직 정렬 */
.main-header th, .sub-header th { 
  position: sticky; z-index: 50; background: #f1f5f9; 
  border-bottom: 1px solid #cbd5e1; border-right: 1px solid #cbd5e1;
  height: 40px; padding: 0; margin: 0; vertical-align: middle;
}
.main-header th { top: 0; }
.sub-header th { top: 40px; z-index: 49; background: #f8fafc; }
.sub-th { font-size: 11px; font-weight: 600; color: #64748b; }

/* 틀 고정 */
.sticky-col { position: sticky; z-index: 40; background: white !important; }
.col-chk { left: 0; width: 50px; }
.col-idx { left: 50px; width: 80px; }
.col-tag { left: 130px; width: 200px; border-right: 3px double #cbd5e1 !important; }
.main-header .sticky-col { z-index: 60 !important; }
.sub-header .sticky-col { z-index: 59 !important; }

.cell-content { min-height: 48px; display: flex; align-items: center; padding: 5px 12px; font-size: 13px; white-space: nowrap; overflow: hidden; }
.tag-text { color: #2563eb; font-weight: bold; }
.group-tbody { border-bottom: 2px solid #94a3b8; }
.cell-pub { background: #f0f9ff; border-right: 1px solid #e2e8f0; border-bottom: 1px solid #e2e8f0; }
.cell-wrk { background: #ffffff; border-right: 1px solid #e2e8f0; border-bottom: 1px solid #e2e8f0; }

/* 3. 모달 (동적 너비 스타일) */
.modal-overlay { position: fixed; inset: 0; background: rgba(15, 23, 42, 0.7); display: flex; align-items: center; justify-content: center; z-index: 1000; }
.modal-content.modal-xl { background: white; width: 95%; max-width: 1600px; border-radius: 10px; overflow: hidden; box-shadow: 0 20px 40px rgba(0,0,0,0.4); }
.modal-header { padding: 15px 25px; background: #f8fafc; border-bottom: 1px solid #e2e8f0; display: flex; justify-content: space-between; align-items: center; }
.header-left { display: flex; align-items: center; gap: 12px; }
.tag-label { background: #1e293b; color: white; padding: 3px 10px; border-radius: 4px; font-weight: bold; font-size: 13px; }
.btn-mode { background: #f1f5f9; border: 1px solid #cbd5e1; padding: 5px 12px; border-radius: 4px; cursor: pointer; font-size: 12px; font-weight: bold; }

.modal-body { padding: 20px; }
.modal-scroll { max-height: 75vh; overflow-y: auto; }

/* 모달 동적 테이블 핵심 스타일 */
.modal-table-wrapper { border: 1px solid #cbd5e1; border-radius: 4px; overflow-x: auto; }
.analysis-table.dynamic-width { width: max-content; min-width: 100%; table-layout: auto; border-collapse: collapse; }
.analysis-table th, .analysis-table td { padding: 12px 15px; border: 1px solid #cbd5e1; text-align: left; white-space: nowrap; }

.h-info { background: #f8fafc; font-weight: bold; text-align: center; }
.h-attr { background: #e0f2fe; color: #0369a1; font-weight: bold; text-align: center; }
.attr-head { background: #f1f5f9; color: #64748b; font-size: 11px; font-family: monospace; }
.text-blue { color: #2563eb; }
.min-w-100 { min-width: 100px; } .min-w-150 { min-width: 150px; } .min-w-120 { min-width: 120px; } .min-w-80 { min-width: 80px; }

/* 배지 및 기타 */
.badge { padding: 2px 8px; border-radius: 12px; font-size: 11px; color: white; font-weight: bold; }
.red { background: #ef4444; } .green { background: #10b981; }
.action-bar { padding: 10px 30px; display: flex; justify-content: space-between; align-items: center; }
.hl { background: #fbbf24; color: black; font-weight: bold; border-radius: 2px; }
.json-viewer { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.json-pane pre { background: #1e293b; color: #38bdf8; padding: 15px; border-radius: 6px; font-size: 11px; overflow: auto; max-height: 60vh; }
</style>
