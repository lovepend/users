<template>
  <div id="app" class="admin-tool">
    <div class="main-layout">
      <header class="dashboard-header">
        <div class="header-container">
          <div class="header-top">
            <h2 class="main-title">Tag Manager <span class="sub-title">Integrated View</span></h2>
          </div>
          <div class="search-grid">
            <div class="project-box">
              <div class="input-group">
                <label>Project No</label>
                <input v-model="projectNo" type="text" placeholder="P0001" class="base-input unified-height project-input" />
              </div>
              <div class="filter-wrapper">
                <label class="sub-label">결과 내 필터</label>
                <input v-model="localSearchQuery" type="text" placeholder="Tag No 검색..." class="filter-input unified-height" />
              </div>
            </div>
            
            <div class="keyword-box">
              <label>API Fetch Keywords</label>
              <textarea v-model="keyword" placeholder="Enter keywords separated by spaces or lines..."></textarea>
            </div>
            
            <div class="button-box">
              <button @click="fetchAndMergeData" :disabled="isLoading" class="btn-fetch-modern">
                <span v-if="!isLoading">조회하기</span>
                <span v-else class="spinner"></span>
              </button>
            </div>
          </div>
        </div>
      </header>

      <div class="action-bar-wrapper">
        <div class="action-bar" v-if="mergedList.length > 0">
          <div class="stats">조회된 그룹: <b>{{ filteredList.length }}</b>건</div>
          <div class="right-buttons">
            <button @click="deleteSelected" class="btn btn-danger" :disabled="selectedIds.length === 0">🗑 선택 삭제</button>
            <button @click="downloadExcel" class="btn btn-success">📥 CSV 내보내기</button>
          </div>
        </div>
      </div>

      <div class="table-card-wrapper">
        <div class="table-container">
          <table class="modern-table">
            <thead>
              <tr class="main-header">
                <th rowspan="2" class="sticky-header sticky-col col-chk border-bottom-none">
                  <div class="center-content"><input type="checkbox" :checked="isAllSelected" @change="toggleAllSelect" /></div>
                </th>
                <th rowspan="2" class="sticky-header sticky-col col-idx border-bottom-none">Idx</th>
                <th rowspan="2" class="sticky-header sticky-col col-tag border-bottom-none">Tag No</th>
                
                <th colspan="3" class="header-group pub-group h-40">Published (Master)</th>
                <th colspan="3" class="header-group wrk-group h-40">Working (Current)</th>
              </tr>
              <tr class="sub-header">
                <th>EP ID</th><th>CLS</th><th>Deleted</th>
                <th>EP ID</th><th>CLS</th><th>Deleted</th>
              </tr>
            </thead>
            
            <tbody v-for="group in filteredList" :key="group.TagIdx" class="group-tbody">
              <tr v-for="(rowNum) in Math.max(group.pubList.length, group.wrkList.length, 1)" :key="group.TagIdx + '-' + rowNum" class="data-row">
                
                <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-chk group-fixed">
                  <div class="cell-content center"><input type="checkbox" :value="group.TagIdx" v-model="selectedIds" /></div>
                </td>
                <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-idx group-fixed clickable" @click="openModal(group)">
                  <div class="cell-content center font-bold text-gray">{{ group.TagIdx }}</div>
                </td>
                <td v-if="rowNum === 1" :rowspan="Math.max(group.pubList.length, group.wrkList.length, 1)" class="sticky-col col-tag group-fixed clickable" @click="openModal(group)">
                  <div class="cell-content tag-text" v-html="highlightText(group.TagNo)"></div>
                </td>

                <td class="cell-pub clickable" @click="openModal(group)"><div class="cell-content">{{ group.pubList[rowNum-1]?.EpId || '-' }}</div></td>
                <td class="cell-pub clickable" @click="openModal(group)"><div class="cell-content">{{ group.pubList[rowNum-1]?.ClsId || '-' }}</div></td>
                <td class="cell-pub clickable" @click="openModal(group)">
                  <div class="cell-content center">
                    <span v-if="group.pubList[rowNum-1]" :class="['badge-modern', group.pubList[rowNum-1].Deleted ? 'is-true' : 'is-false']">
                      {{ group.pubList[rowNum-1].Deleted }}
                    </span>
                    <span v-else>-</span>
                  </div>
                </td>
                <td class="cell-wrk clickable" @click="openModal(group)"><div class="cell-content">{{ group.wrkList[rowNum-1]?.EpId || '-' }}</div></td>
                <td class="cell-wrk clickable" @click="openModal(group)"><div class="cell-content">{{ group.wrkList[rowNum-1]?.ClsId || '-' }}</div></td>
                <td class="cell-wrk clickable" @click="openModal(group)">
                  <div class="cell-content center">
                    <span v-if="group.wrkList[rowNum-1]" :class="['badge-modern', group.wrkList[rowNum-1].Deleted ? 'is-true' : 'is-false']">
                      {{ group.wrkList[rowNum-1].Deleted }}
                    </span>
                    <span v-else>-</span>
                  </div>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <div v-if="showModal" class="modal-overlay" @click.self="closeModal">
      <div class="modal-content-modern">
        <div class="modal-header-modern">
          <h3>Detail Analysis <small>#{{ selectedItem?.TagNo }}</small></h3>
          <button @click="closeModal" class="btn-close-modern">&times;</button>
        </div>
        <div class="modal-body-modern">
          <div class="modal-grid-modern">
            <div class="data-pane"><h4 class="pub-title-modal">Published Data</h4><pre>{{ JSON.stringify(selectedItem?.pubList, null, 2) }}</pre></div>
            <div class="data-pane"><h4 class="wrk-title-modal">Working Data</h4><pre>{{ JSON.stringify(selectedItem?.wrkList, null, 2) }}</pre></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
// ... (Script 로직은 이전과 완벽히 동일하며, openModal 함수 등 리턴 확인 완료)
import { ref, computed } from 'vue'
import { axiosWrapper } from '@/utils/axios/axios-wrapper'

export default {
  setup() {
    const projectNo = ref('')
    const keyword = ref('')
    const localSearchQuery = ref('')
    const mergedList = ref([])
    const isLoading = ref(false)
    const selectedIds = ref([])
    const showModal = ref(false)
    const selectedItem = ref(null)

    const fetchAndMergeData = async () => {
      if (!keyword.value.trim()) return alert('키워드를 입력해주세요.');
      isLoading.value = true;
      selectedIds.value = [];
      const groupMap = new Map();

      try {
        const keywords = keyword.value.split(/[\s,\n]+/).map(k => k.trim()).filter(k => k);
        await Promise.all(keywords.map(async (key) => {
          const payload = { ProjectNo: projectNo.value, Keyword: key };
          const [pubRes, wrkRes] = await Promise.all([
            axiosWrapper.post('/api/Admin/Tool/GetPublishedData', payload),
            axiosWrapper.post('/api/Admin/Tool/GetWorkingData', payload)
          ]);

          const pData = Array.isArray(pubRes?.data) ? pubRes.data : (Array.isArray(pubRes) ? pubRes : []);
          const wData = Array.isArray(wrkRes?.data) ? wrkRes.data : (Array.isArray(wrkRes) ? wrkRes : []);

          pData.forEach(item => {
            if (!groupMap.has(item.TagIdx)) groupMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pubList: [], wrkList: [] });
            groupMap.get(item.TagIdx).pubList.push(item);
          });
          wData.forEach(item => {
            if (!groupMap.has(item.TagIdx)) groupMap.set(item.TagIdx, { TagIdx: item.TagIdx, TagNo: item.TagNo, pubList: [], wrkList: [] });
            groupMap.get(item.TagIdx).wrkList.push(item);
          });
        }));
        mergedList.value = Array.from(groupMap.values()).sort((a, b) => String(a.TagNo).localeCompare(String(b.TagNo)));
      } catch (e) { alert('데이터 로드 실패'); } finally { isLoading.value = false; }
    };

    const filteredList = computed(() => {
      if (!localSearchQuery.value.trim()) return mergedList.value;
      const q = localSearchQuery.value.toLowerCase();
      return mergedList.value.filter(g => String(g.TagNo).toLowerCase().includes(q));
    });

    const highlightText = (text) => {
      if (!localSearchQuery.value.trim()) return text;
      const regex = new RegExp(`(${localSearchQuery.value.trim()})`, 'gi');
      return String(text).replace(regex, '<mark class="hl">$1</mark>');
    };

    const toggleAllSelect = () => {
      if (selectedIds.value.length === filteredList.value.length) selectedIds.value = [];
      else selectedIds.value = filteredList.value.map(g => g.TagIdx);
    };

    return {
      projectNo, keyword, localSearchQuery, mergedList, filteredList, isLoading,
      selectedIds, showModal, selectedItem,
      fetchAndMergeData, highlightText, toggleAllSelect,
      openModal: (g) => { selectedItem.value = g; showModal.value = true; },
      closeModal: () => { showModal.value = false; },
      isAllSelected: computed(() => filteredList.value.length > 0 && selectedIds.value.length === filteredList.value.length),
      deleteSelected: () => confirm(`선택한 ${selectedIds.value.length}건을 삭제하시겠습니까?`),
      downloadExcel: () => alert('CSV 다운로드 기능을 실행합니다.')
    };
  }
}
</script>

<style scoped>
/* [디자인 전면 개편] 전체 레이아웃 */
.main-layout { height: 100vh; display: flex; flex-direction: column; background: #f4f7fa; color: #334155; font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; overflow: hidden; }

/* 1. 모던 대시보드 헤더 */
.dashboard-header { background: white; padding: 25px 35px; border-bottom: 1px solid #e2e8f0; box-shadow: 0 4px 15px rgba(0,0,0,0.03); flex-shrink: 0; z-index: 10; }
.header-container { max-width: 100%; margin: 0 auto; }
.header-top .main-title { margin: 0 0 15px 0; font-size: 1.6rem; color: #1e293b; font-weight: 800; display: flex; align-items: center; gap: 10px; }
.sub-title { font-size: 0.8rem; background: #e0f2fe; color: #0369a1; padding: 3px 10px; border-radius: 12px; font-weight: 600; }

.search-grid { display: flex; gap: 25px; align-items: flex-start; }
.project-box { width: 220px; display: flex; flex-direction: column; gap: 12px; }
.unified-height { height: 38px !important; } 

.base-input { border: 1px solid #cbd5e1; border-radius: 6px; padding: 0 12px; font-size: 0.9rem; transition: 0.2s; }
.base-input:focus { border-color: #3b82f6; box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1); outline: none; }
.filter-input { border: 1px solid #3b82f6; background: #eff6ff; }

.keyword-box { flex: 1; }
.keyword-box textarea { width: 100%; height: 45px; border: 1px solid #cbd5e1; border-radius: 6px; padding: 10px; resize: none; transition: 0.2s; }
.keyword-box textarea:focus { height: 100px; }

.btn-fetch-modern { min-width: 160px; height: 45px; background: #1e293b; color: white; border-radius: 6px; font-weight: bold; border: none; cursor: pointer; transition: 0.2s; }
.btn-fetch-modern:hover { background: #334155; }

/* 2. 슬림 액션바 */
.action-bar-wrapper { padding: 10px 35px; flex-shrink: 0; }
.action-bar { display: flex; justify-content: space-between; align-items: center; padding: 8px 15px; background: white; border-radius: 8px; border: 1px solid #e2e8f0; }
.stats { font-size: 0.85rem; color: #64748b; }
.right-buttons { display: flex; gap: 8px; }
.btn { padding: 7px 14px; border-radius: 4px; border: none; font-weight: 600; cursor: pointer; font-size: 0.8rem; }
.btn-danger { background: #fee2e2; color: #dc2626; } .btn-danger:hover { background: #fecaca; }
.btn-success { background: #d1fae5; color: #059669; } .btn-success:hover { background: #a7f3d0; }

/* 3. 모던 테이블 컨테이너 (그림자 효과) */
.table-card-wrapper { flex: 1; overflow: auto; margin: 0 35px 25px; background: white; border-radius: 12px; box-shadow: 0 10px 30px rgba(0,0,0,0.05); border: 1px solid #e2e8f0; position: relative; }
.table-container { height: 100%; width: 100%; overflow: auto; }
.modern-table { width: 100%; border-collapse: separate; border-spacing: 0; table-layout: fixed; }

/* 🛠 높이 고정 (스타일 수정) */
.h-40 { height: 40px !important; padding: 0 !important; }

/* 상단 헤더 (z-index 50) */
.main-header th { position: sticky; top: 0; z-index: 50; background: #f8fafc; color: #475569; font-weight: 700; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.5px; border-bottom: 1px solid #e2e8f0; border-right: 1px solid #e2e8f0; }
.sub-header th { position: sticky; top: 40px; z-index: 49; background: #f8fafc; border-bottom: 2px solid #cbd5e1; border-right: 1px solid #e2e8f0; font-size: 0.75rem; color: #64748b; }

/* 좌측 열 틀 고정 (z-index 40) */
.sticky-col { position: sticky; z-index: 40; background: white !important; }
.border-right { border-right: 1px solid #e2e8f0; }
.col-chk { left: 0; width: 60px; }
.col-idx { left: 60px; width: 80px; }
.col-tag { left: 140px; width: 220px; }

/* 모서리 (z-index 최상위) */
.main-header .sticky-col { z-index: 60 !important; top: 0; }
.sub-header .sticky-col { z-index: 59 !important; top: 40px; }

/* 데이터 셀 가독성 및 그룹 구분 */
.cell-content { min-height: 55px; display: flex; align-items: center; padding: 8px 15px; box-sizing: border-box; font-size: 0.9rem; }
.center, .center-content { justify-content: center; display: flex; align-items: center; width: 100%; }

.tag-text { color: #2563eb; font-weight: 600; font-family: monospace; font-size: 0.95rem; }
.group-fixed { background: #fcfcfd !important; border-right: 1px solid #e2e8f0; }
.border-bold-right { border-right: 2px solid #cbd5e1 !important; }

/* 은은한 데이터 영역 배경색 */
.cell-pub { background: #f0f9ff; border-bottom: 1px solid #e0f2fe; }
.cell-wrk { background: #fffdfa; border-bottom: 1px solid #fff3e0; }

/* 모던 Badge (톤다운) */
.badge-modern { padding: 4px 12px; border-radius: 12px; font-size: 0.7rem; font-weight: 700; color: white; display: inline-block; text-transform: uppercase; letter-spacing: 0.5px; }
.is-true { background: #f87171; } /* True: 은은한 빨간색 */
.is-false { background: #34d399; } /* False: 은은한 초록색 */

.group-tbody { border-bottom: 2px solid #e2e8f0; }
.clickable { cursor: pointer; transition: 0.1s; }
.clickable:hover { background-color: #f1f5f9; }

/* 모달 디자인 고급화 */
.modal-overlay { position: fixed; inset: 0; background: rgba(15, 23, 42, 0.7); display: flex; align-items: center; justify-content: center; z-index: 1000; backdrop-filter: blur(5px); }
.modal-content-modern { background: white; width: 1000px; border-radius: 16px; overflow: hidden; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25); }
.modal-header-modern { padding: 20px 25px; border-bottom: 1px solid #e2e8f0; display: flex; justify-content: space-between; align-items: center; }
.modal-header-modern h3 { margin: 0; font-size: 1.25rem; }
.modal-body-modern { padding: 30px; max-height: 70vh; overflow-y: auto; background: #fafafa; }
.modal-grid-modern { display: grid; grid-template-columns: 1fr 1fr; gap: 25px; }
.data-pane pre { background: #1e293b; color: #e2e8f0; padding: 20px; border-radius: 10px; font-size: 0.8rem; overflow: auto; line-height: 1.6; }
.btn-close-modern { background: none; border: none; font-size: 32px; cursor: pointer; color: #94a3b8; }

::v-deep .hl { background: #fbbf24; color: black; font-weight: bold; border-radius: 2px; padding: 0 2px; }
</style>
