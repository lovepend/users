// 기존 gridBindRef 코드를 찾아 아래 내용을 추가하세요
const gridBindRef = ref({
  key: gridKeyRef,
  columnDefs: gridColDefsRef,
  rowData: gridDataRef,
  class: 'ag-theme-custom w-100 h-100',
  
  // --- 이 부분을 추가하세요 ---
  initialState: {
    sort: {
      sortModel: [
        { colId: 'SEQ', sort: 'asc' } // SEQ 컬럼을 오름차순(asc)으로 정렬
      ],
    },
  },
  // -------------------------

  rowSelection: 'multiple',
  rowDragManaged: true,
  // ... 이하 생략
});
