<template>
  <main class="flex-grow-1 min-h-0 p-2">
    <DefaultCard :tabs="tabList" :title="null" v-model:active-tab="activeTab" class="h-100 grid-card">
      <div class="tab-content-wrapper">
        <keep-alive>
          <component :is="tabs[activeTab]" :active="activeTab" class="h-100" />
        </keep-alive>
      </div>
    </DefaultCard>
  </main>
</template>

<script setup>
  import { ref } from 'vue'
  import DefaultCard from '@/components/default/DefaultCard.vue'

  import TagTab from './components/TagTab.vue'

  import TestTab from './components/TestTab.vue'
  // import TagTypeTab from './components/TagTypeTab.vue'
  // import MergeTab from './components/MergeTab.vue'

  const activeTab = ref('test')

  const tabList = [
    { id: 'tag', name: 'tag', icon: 'fa-layer-group' },
    { id: 'test', name: 'test', icon: 'fa-address-card' },
    // { id: 'tagtype', name: 'tagtype', icon: 'fa-code' },
    // { id: 'merge', name: 'merge', icon: 'fa-shapes' },
  ]

  const tabs = {
    tag: TagTab,

    test: TestTab,
    // tagtype: TagTypeTab,
    // merge: MergeTab,
  }
</script>

<style scoped>
  .main-layout-container {
    height: 100vh;
  }

  .min-h-0 {
    min-height: 0;
  }

  :deep(.grid-card) {
    display: flex;
    flex-direction: column;
    border-radius: 8px;
  }

  :deep(.card__contents) {
    flex-grow: 1;
    min-height: 0;
    padding: 0 !important;
    display: flex;
    flex-direction: column;
  }

  .tab-content-wrapper {
    flex-grow: 1;
    height: 100%;
    width: 100%;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .tab-content-wrapper > * {
    flex-grow: 1;
    min-height: 0;
  }
</style>
