// 변경 전
const res = await fetch('./build-meta.json')

// 변경 후 (쿼리스트링을 붙여 브라우저 캐시를 강제로 우회)
const res = await fetch(`./build-meta.json?t=${new Date().getTime()}`)





import { useVersionCheck } from '@/stores/app-version-check'
import { onMounted } from 'vue'

const versionCheck = useVersionCheck()
onMounted(() => {
  versionCheck.start() // 여기서 켜야 메인이든 어디든 접속하자마자 돕니다.
})
