<span class="text-truncate">{{ selectedProjects.length > 0 ? `${selectedProjects.length}개 선택됨` : '선택하세요' }}</span>

<span class="text-truncate">
  {{ selectedProjects.length > 0 ? selectedProjects.join(', ') : '프로젝트를 선택하세요' }}
</span>
