// AS-IS: 프로젝트 선택 여부를 무조건 체크함
const onCreateRows = function () {
  if (!currentProjNo.value) {
    sAlert.Warning(`Project를  선택해주세요`)
  } else {
    insertRows(1)
  }
}

// TO-BE: 체크 조건을 지우고 바로 행 추가가 가능하도록 변경
const onCreateRows = function () {
  insertRows(1)
}

const onCreateMultiRows = function (rowCount) {
  insertRows(rowCount)
}
