// 파일 상단에 이 부분이 잘 설정되어 있는지 확인하세요!
import { ModuleRegistry } from '@ag-grid-community/core';
import { ClientSideRowModelModule } from '@ag-grid-community/client-side-row-model';
import { MenuModule } from '@ag-grid-enterprise/menu'; // 👈 우클릭 메뉴용 모듈
import { ExcelExportModule } from '@ag-grid-enterprise/excel-export'; // 👈 엑셀 추출용 모듈

ModuleRegistry.registerModules([
  ClientSideRowModelModule,
  MenuModule,
  ExcelExportModule
]);
