<template>
  <DefaultCard
    :title="exampleTitle"
    class="w-100 h-100"
  >
    <template #header-right>
      <div class="d-flex align-items-center gap-2">
        <InputBox
          id="input-proj"
          label="프로젝트"
          v-model:value="projectCode"
          placeholder="코드 입력"
          style="width: 140px"
        />
        <InputBox
          id="input-keyword"
          label="키워드"
          v-model:value="keyword"
          placeholder="키워드 입력"
          style="width: 140px"
        />
        <InputBox
          id="input-search"
          label="검색"
          v-model:value="attributeFilter"
          icon="fa-search"
          placeholder="검색어 입력"
          style="width: 180px"
          @clear="attributeFilter = ''"
        />

        <div class="header-divider"></div>

        <button 
          type="button" 
          class="tpl-btn tpl-btn--sm tpl-btn--primary" 
          @click="confirmSearch"
        >
          조회
        </button>
      </div>
    </template>

    <div class="grid-wrapper w-100 h-100">
      <ag-grid-vue
        class="ag-theme-custom w-100 h-100"
        v-bind="gridOptions"
        @grid-ready="onGridReady"
      />
    </div>
  </DefaultCard>
</template>

<script setup>
import { ref, computed, shallowRef } from 'vue'
import { axiosWrapper } from '@/utils/axios/axios-wrapper'
import { useToast } from '@/composables/useToast'

// 템플릿 공통 컴포넌트 임포트 [cite: 68]
import DefaultCard from '@/components/default/DefaultCard.vue'
import InputBox from '@/components/default/editors/InputBox.vue'
import AggridLoadingOverlay from '@/components/aggrid/AgGridLoadingOverlay.vue'

const toast = useToast()

/**
 * 템플릿 예시 1번()과 완전히 동일한 아이콘 및 타이틀 구성
 */
const exampleTitle = {
  icon: 'fa-list', // 기존 템플릿과 동일한 아이콘 
  title: '재고 현황',
  subtitle: '창고별 집계 · 실시간 반영', // 기존 템플릿 문구 유지 
}

const projectCode = ref('')
const keyword = ref('')
const attributeFilter = ref('')

const gridApi = shallowRef()
const gridRowData = ref([])
const columnDefs = ref([])

/**
 * Toast "확인/취소" 조회 컨펌 [cite: 67, 88]
 */
const confirmSearch = () => {
  toast.show({
    title: '조회 확인',
    content: '데이터를 조회하시겠습니까?',
    okText: '확인',
    cancelText: '취소',
    onOk: () => fetchData()
  })
}

const fetchData = async () => {
  if (gridApi.value) gridApi.value.setGridOption('loading', true)
  try {
    const res = await axiosWrapper.post('/api/Inventory/GetStatus', { 
      ProjectNo: projectCode.value,
      Keyword: keyword.value 
    })
    gridRowData.value = Array.isArray(res) ? res : []
    
    if (gridRowData.value.length > 0) {
      columnDefs.value = Object.keys(gridRowData.value[0]).map(key => ({
        headerName: key, field: key, sortable: true, filter: true, resizable: true, flex: 1
      }))
    }
  } catch (error) {
    toast.fail({ title: '오류', content: '데이터 조회 실패' })
  } finally {
    if (gridApi.value) gridApi.value.setGridOption('loading', false)
  }
}

const gridOptions = computed(() => ({
  columnDefs: columnDefs.value,
  rowData: gridRowData.value,
  loadingOverlayComponent: AggridLoadingOverlay,
  quickFilterText: attributeFilter.value
}))

const onGridReady = (params) => {
  gridApi.value = params.api
}
</script>

<style scoped>
.w-100 { width: 100%; }
.h-100 { height: 100%; }

.grid-wrapper {
  display: flex;
  flex-direction: column;
  flex: 1;
  min-height: 0;
  padding: 0; 
}

/* 템플릿 스타일 가이드 반영 [cite: 89] */
.header-divider {
  width: 1px;
  height: 14px;
  background-color: var(--tpl-divider, rgba(0, 0, 0, 0.08));
  margin: 0 10px;
}

:deep(.input-box-container) {
  margin-bottom: 0;
}

.ag-theme-custom {
  border: 1px solid var(--tpl-divider);
  overflow: hidden;
}
</style>
