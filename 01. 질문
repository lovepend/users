<template>
  <div class="adapter-compare-page h-100 w-100 d-flex flex-column bg-white">
    <DefaultCard
      :title="{ 
        icon: 'fa-project-diagram', 
        title: 'Adapter Mapping 비교 분석', 
        subtitle: '프로젝트 간 매핑 차이를 분석하고 상세 내역을 대조합니다.' 
      }"
      class="flex-grow-1"
      body-class="p-0 d-flex flex-column"
    >
      <template #header-right>
        <div class="d-flex align-items-center gap-2 flex-wrap">
          <div class="d-flex align-items-center gap-2">
            <span class="fw-bold text-dark text-nowrap small">대상 프로젝트</span>
            <div class="dropdown">
              <div class="ui-tag-tab-box dropdown-toggle" data-bs-toggle="dropdown" data-bs-auto-close="outside">
                <span class="text-truncate">{{ selectedProjects.length > 0 ? selectedProjects.join(', ') : '프로젝트 선택' }}</span>
              </div>
              <ul class="dropdown-menu p-2 shadow-lg border-0" style="width: 300px; max-height: 400px; overflow-y: auto; z-index: 1050;">
                <li class="px-2 pb-2 border-bottom mb-1">
                  <input type="text" class="form-control form-control-sm" v-model="projectSearchKeyword" placeholder="검색..." @click.stop />
                </li>
                <li v-for="proj in filteredProjectList" :key="proj.PROJ_NO" class="dropdown-item px-2 py-1">
                  <label class="form-check-label d-flex align-items-center w-100 cursor-pointer small text-nowrap">
                    <input class="form-check-input me-2 mt-0" type="checkbox" :value="proj.PROJ_NO" v-model="selectedProjects" />
                    <span class="text-truncate">{{ proj.PROJ_NO }}</span>
                  </label>
                </li>
              </ul>
            </div>
          </div>
          <button class="ui-tag-tab-search-btn shadow-sm" @click="onSearch" :disabled="selectedProjects.length === 0">조회</button>
          <div class="header-divider mx-2"></div>
          <div class="btn-group p-1 bg-secondary bg-opacity-10 rounded-pill border">
            <input type="radio" class="btn-check" id="modeS" value="simple" v-model="viewMode" /><label class="btn btn-sm rounded-pill px-3 toggle-label" for="modeS">간편</label>
            <input type="radio" class="btn-check" id="modeD" value="detail" v-model="viewMode" /><label class="btn btn-sm rounded-pill px-3 toggle-label" for="modeD">상세</label>
          </div>
          <div class="header-divider mx-2"></div>
          <textarea class="ui-tag-tab-textarea" v-model="attributeFilter" placeholder="MAP_ID 필터..."></textarea>
        </div>
      </template>

      <div class="grid-wrapper flex-grow-1 border-top bg-white">
        <ag-grid-vue
          style="width: 100%; height: 100%;"
          class="ag-theme-custom"
          :columnDefs="mainColumnDefs"
          :rowData="gridRowData"
          :defaultColDef="defaultColDef"
          @grid-ready="onGridReady"
          @cell-clicked="onCellClicked"
        />
      </div>
    </DefaultCard>

    <div class="modal fade custom-top-modal" id="arrayDiffModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
      <div class="modal-dialog modal-xl shadow-lg border-0">
        <div class="modal-content border-0 overflow-hidden">
          <div class="modal-header py-2 bg-dark text-white border-0">
            <h6 class="modal-title small">
              <i class="fa fa-copy me-2"></i>상세 비교 분석 [<span class="text-warning">{{ selectedMapId }}</span>]
            </h6>
            <button type="button" class="btn-close btn-close-white shadow-none" data-bs-dismiss="modal"></button>
          </div>
          <div class="modal-body p-0 d-flex flex-column" style="height: 60vh; background: #fff;">
            <div class="bg-light p-2 border-bottom d-flex gap-1">
              <button 
                v-for="t in ['MAP_ATT', 'MAP_CLS', 'MAP_TMPLS', 'MAP_RELS']" 
                :key="t" 
                class="btn btn-xs px-3" 
                :class="activeTab === t ? 'btn-primary fw-bold' : 'btn-white border'" 
                @click="activeTab = t"
              >
                {{ t }}
              </button>
            </div>
            <div class="flex-grow-1 position-relative">
              <ag-grid-vue
                style="width: 100%; height: 100%; position: absolute;"
                class="ag-theme-custom"
                :columnDefs="modalColumnDefs"
                :rowData="modalRowData"
                :defaultColDef="defaultColDef"
              />
            </div>
          </div>
          <div class="modal-footer py-1 bg-light border-top">
            <button type="button" class="btn btn-secondary btn-xs px-3" data-bs-dismiss="modal">Close</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped lang="scss">
.adapter-compare-page { height: 100%; overflow: hidden; }

.grid-wrapper {
  position: relative;
  width: 100%;
  height: 100%;
}

/* UI 요소 스타일 */
.ui-tag-tab-box { height: 32px; border: 1px solid #ddd; padding: 0 10px; cursor: pointer; display: flex; align-items: center; background: #fff; border-radius: 4px; font-size: 0.85rem; }
.ui-tag-tab-search-btn { height: 32px; background: #0d6efd; color: #fff; border: none; border-radius: 4px; padding: 0 15px; font-weight: 600; font-size: 0.85rem; }
.ui-tag-tab-textarea { height: 32px; border: 1px solid #ddd; border-radius: 4px; padding: 5px 10px; resize: none; width: 220px; font-size: 0.8rem; }
.header-divider { width: 1px; height: 18px; background: #dee2e6; }
.toggle-label { cursor: pointer; font-size: 0.8rem; font-weight: 600; padding: 4px 12px; color: #6c757d; }
.btn-check:checked + .toggle-label { background-color: #fff !important; color: #0d6efd !important; box-shadow: 0 1px 2px rgba(0,0,0,0.1); }

/* [해결] 모달 레이어 및 위치 수정 */
.custom-top-modal {
  /* 부트스트랩 기본값보다 높게 설정하여 모든 오버레이보다 위로 올림 */
  z-index: 2000 !important; 
  
  .modal-dialog {
    /* 요청하신 대로 상단에서 200px 아래로 배치 */
    margin-top: 200px !important; 
    transition: margin-top 0.3s ease-out;
  }

  .modal-content {
    border-radius: 8px;
    /* tagtab.vue 스타일의 그림자 강조 */
    box-shadow: 0 1rem 3rem rgba(0, 0, 0, 0.3) !important;
  }
}

/* 백드롭(회색 배경)의 순서도 모달 바로 아래로 조정 */
:deep(.modal-backdrop) {
  z-index: 1999 !important;
}

:deep(.ag-theme-custom) {
  --ag-header-background-color: #f8f9fa;
  --ag-font-size: 12px;
  .ag-header-group-cell {
    background-color: #e9ecef !important;
    font-weight: 800 !important;
    border-right: 1px solid #dee2e6 !important;
  }
}

.btn-xs { padding: 0.25rem 0.6rem; font-size: 0.75rem; border-radius: 4px; }
</style>
