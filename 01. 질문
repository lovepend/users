{
  field: 'EP_ID',
  headerName: 'EP',
  minWidth: 60,
  flex: 1,
  editable: params => { ... },
  cellEditor: 'agRichSelectCellEditor',
  // 에디터가 열려있을 때 그리드 본체의 키보드 이벤트를 제어
  suppressKeyboardEvent: params => {
    // 편집 모드(editing)일 때는 그리드 자체의 위아래 이동 기본 동작을 막아줌
    return params.editing; 
  },
  cellRenderer: params => makeComboRender(params),
}
