// 우클릭 메뉴 제어 함수
const getContextMenuItems = (params) => {
  const defaultItems = params.defaultItems || []
  // 'excelExport'와 하위 자식 메뉴를 포함한 'export' 묶음 통째로 제거
  return defaultItems.filter(item => item !== 'excelExport' && item !== 'export')
}
