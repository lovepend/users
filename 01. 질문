const getMapDatas = async itemAdapterDataMapping => {
    gridApi_MapAttr.value.setGridOption('loading', true)
    gridApi_MapClass.value.setGridOption('loading', true)
    gridApi_MapTmpl.value.setGridOption('loading', true)
    gridApi_MapRel.value.setGridOption('loading', true)

    try {
      let dataMessage = ''

      selectedBypassRef.value = itemAdapterDataMapping.BYPASS ? itemAdapterDataMapping.BYPASS : false
      selectedBypassPublishRef.value = itemAdapterDataMapping.BYPASS_PUBLISH
        ? itemAdapterDataMapping.BYPASS_PUBLISH
        : false
